# Summarize a boolean county-agreement vector into the shape consumed by `.linkage_validate_geocode_checks()`.

Summarize a boolean county-agreement vector into the shape consumed by
[`.linkage_validate_geocode_checks()`](https://joonho112.github.io/ALprekDB/reference/dot-linkage_validate_geocode_checks.md).

## Usage

``` r
.linkage_county_summarize(v, src_label)
```
