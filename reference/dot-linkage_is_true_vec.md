# Logical TRUE-ness with NA tolerance, vectorized. Returns FALSE for NA. Mirrors `isTRUE()` semantics extended to vectors.

Logical TRUE-ness with NA tolerance, vectorized. Returns FALSE for NA.
Mirrors [`isTRUE()`](https://rdrr.io/r/base/Logic.html) semantics
extended to vectors.

## Usage

``` r
.linkage_is_true_vec(x)
```
