# Build a `tibble` of group counts + percentages for a categorical column.

Build a `tibble` of group counts + percentages for a categorical column.

## Usage

``` r
.linkage_geocode_group_pct(x, n_total, col_name)
```
