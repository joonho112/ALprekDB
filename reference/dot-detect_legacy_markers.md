# Detect legacy format markers

Detect legacy format markers

## Usage

``` r
.detect_legacy_markers(col_names)
```

## Arguments

- col_names:

  Character vector of column names.

## Value

Logical.
