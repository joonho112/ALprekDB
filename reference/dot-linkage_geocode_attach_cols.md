# Canonical list of the 12 prefixed geocode columns we attach onto the classroom-level master. Mirrors `.lg_geocode_attach_cols()` in R/linkage-geocode.R; redeclared here so master callers don't depend on that file's internal helper signature.

Canonical list of the 12 prefixed geocode columns we attach onto the
classroom-level master. Mirrors `.lg_geocode_attach_cols()` in
R/linkage-geocode.R; redeclared here so master callers don't depend on
that file's internal helper signature.

## Usage

``` r
.linkage_geocode_attach_cols()
```
