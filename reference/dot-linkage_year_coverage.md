# Build two-panel year coverage metadata

Build two-panel year coverage metadata

## Usage

``` r
.linkage_year_coverage(
  left_years,
  right_years,
  left_label = "left",
  right_label = "right"
)
```
