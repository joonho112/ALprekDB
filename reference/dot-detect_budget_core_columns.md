# Detect core processed classroom-budget identifier columns

Detect core processed classroom-budget identifier columns

## Usage

``` r
.detect_budget_core_columns(col_names)
```

## Arguments

- col_names:

  Character vector of column names.

## Value

Logical.
