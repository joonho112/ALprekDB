# Find the classroom code column in raw data

Find the classroom code column in raw data

## Usage

``` r
.find_classroom_code_col(col_names)
```

## Arguments

- col_names:

  Character vector of column names.

## Value

Column name or NULL.
