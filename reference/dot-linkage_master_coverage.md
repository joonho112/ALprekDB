# Build three-panel year coverage metadata for linked master objects

Build three-panel year coverage metadata for linked master objects

## Usage

``` r
.linkage_master_coverage(budget, classroom, student)
```
