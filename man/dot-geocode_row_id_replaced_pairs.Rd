% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-detect.R
\name{.geocode_row_id_replaced_pairs}
\alias{.geocode_row_id_replaced_pairs}
\title{Detect resolved row_id pairs (\verb{_new} placeholder → assigned site_code)}
\usage{
.geocode_row_id_replaced_pairs(removed_rows, added_rows)
}
\arguments{
\item{removed_rows, added_rows}{Tibbles with at least the columns
\code{row_id}, \code{school_year}, \code{site_name}, \code{geocode_address},
\code{site_code} (the latter may be \code{NA} in \verb{_new} rows).}
}
\value{
A tibble with columns: \code{old_row_id}, \code{new_row_id},
\code{school_year_old}, \code{school_year_new}, \code{site_name},
\code{geocode_address}, \code{assigned_site_code}.
}
\description{
Joins removed (only_old) rows whose school_year ends in
\verb{_new} against added (only_new) rows whose school_year is the same
root WITHOUT \verb{_new}, on the natural keys
\verb{(school_year_root, site_name, geocode_address)}. Each successful
join is reported as one row in the returned tibble. The matching
semantics match Step 3.5 of the protocol (\code{row_id_replaced} pairs).
}
\keyword{internal}
