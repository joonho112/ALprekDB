% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-validate.R
\name{geocode_validate}
\alias{geocode_validate}
\title{Validate Cleaned Geocode Data}
\usage{
geocode_validate(clean, strict = FALSE, config = NULL)
}
\arguments{
\item{clean}{An \code{alprek_geocode_clean} object from \code{\link[=geocode_clean]{geocode_clean()}}.}

\item{strict}{Logical. If \code{TRUE}, treats warnings as overall failure.
Default \code{FALSE}.}

\item{config}{Optional \code{alprek_geocode_config} (from \code{\link[=geocode_config]{geocode_config()}}).
When provided, supplies \code{al_lat_bounds} / \code{al_lng_bounds}. RESULTCODE
canonicality remains fixed to the documented Melissa set \code{{GS01..GS08}};
master acceptability is enforced later from the RESULTCODE codebook.}
}
\value{
An \code{alprek_geocode_validation} S3 list with elements:
\itemize{
\item \code{passed}: logical (overall result).
\item \code{n_errors}, \code{n_warnings}, \code{n_info}: integer counts.
\item \code{checks}: tibble with columns \code{check_id}, \code{description}, \code{status},
\code{n_issues}, \code{details}.
\item \code{issues}: tibble with columns \code{row_id}, \code{check_id}, \code{severity},
\code{value}, \code{expected}, \code{note}.
}
}
\description{
Comprehensive data-quality checks on an
\code{alprek_geocode_clean} object (output of \code{\link[=geocode_clean]{geocode_clean()}}). Mirrors
the API of \code{\link[=applications_validate]{applications_validate()}}, \code{\link[=budget_validate]{budget_validate()}}, and
\code{\link[=classroom_validate]{classroom_validate()}}: each check is logged with a structured row
(\code{check_id}, \code{description}, \code{status} one of \code{PASS}/\code{ERROR}/\code{WARN}/\code{INFO},
\code{n_issues}, \code{details}), and offending rows accumulate in \verb{$issues}.

Validation is scoped to the \strong{data contract layer}: column existence,
value ranges, AL geographic bounds, codebook membership, key
consistency, provenance. Per-row coordinate reconciliation (ADECE vs
Melissa, distance-tier-driven decisions, follow-up routing) is the
responsibility of \code{\link[=geocode_reconcile]{geocode_reconcile()}} in Phase 4.
\subsection{15 Checks}{
\enumerate{
\item \code{required_columns} (ERROR): all 29 expected columns present.
\item \code{row_id_unique} (ERROR): \code{row_id} has no duplicates.
\item \code{row_id_format} (WARN): \code{row_id} matches \verb{\{YYYY-YYYY\}_\{site_code\}}
or \verb{\{YYYY-YYYY\}_new_NNNN}.
\item \code{school_year_canonical} (ERROR): values in the canonical 5-level
set.
\item \code{site_code_missingness_in_new_only} (ERROR): \code{site_code} NA rows
must all carry \code{school_year == "*_new"}.
\item \code{melissa_lat_lng_present} (ERROR): Melissa \code{LAT}/\code{LNG} are 100\%
non-NA.
\item \code{has_latlon_consistency} (ERROR): \code{has_latlon == !is.na(latitude)}.
\item \code{melissa_coord_in_al_bounds} (ERROR): non-NA Melissa coords inside
AL bounding box.
\item \code{adece_coord_in_al_bounds} (WARN): non-NA ADECE coords inside AL
bounding box.
\item \code{resultcode_canonical} (WARN): RESULTCODE in \code{{GS01..GS08}}.
\item \code{statuscode_canonical} (WARN): STATUSCODE in observed codebook.
\item \code{resultcode_statuscode_consistency} (WARN): 1:1 pairings observed
in the STATUSCODE codebook.
\item \code{errorcode_all_na_in_v080} (INFO): all \code{ERRORCODE} values are NA
(v0.8.0 contract; future deliveries may populate).
\item \code{provenance_complete} (ERROR): \code{meta} carries \code{file_sha256},
\code{cycle_year}, \code{receipt_date}, \code{git_sha}.
\item \code{lineage_id_complete} (ERROR): row-level \code{lineage_id} exists,
is non-blank, and is unique.
}

Plus a final \code{summary_coverage} (INFO) check reporting RESULTCODE
coverage \%, follow-up queue size estimate, and PLACENAME missingness.
}
}
\examples{
\dontrun{
raw <- geocode_read(path, cycle_year = "2026-2027",
                    receipt_date = "2026-03-04")
clean <- geocode_clean(raw)
v <- geocode_validate(clean)
print(v)
v$checks
v$issues
}

}
\seealso{
\code{\link[=geocode_clean]{geocode_clean()}}, \code{\link[=geocode_config]{geocode_config()}},
\code{\link[=alprek_geocode_al_fips_counties]{alprek_geocode_al_fips_counties()}},
\code{\link[=alprek_geocode_resultcode_meaning]{alprek_geocode_resultcode_meaning()}}.
}
