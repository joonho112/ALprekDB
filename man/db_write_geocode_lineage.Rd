% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-geocode.R
\name{db_write_geocode_lineage}
\alias{db_write_geocode_lineage}
\title{Write a geocode lineage row to DuckDB}
\usage{
db_write_geocode_lineage(conn, x, run_id = NULL)
}
\arguments{
\item{conn}{A DBI connection.}

\item{x}{One of \code{alprek_geocode_clean}, \code{alprek_geocode_reconciled},
\code{alprek_geocode_master}, or \code{alprek_geocode_panel}.}

\item{run_id}{Optional character — override the derived
\code{geocode_run_id} (only honored when \code{x} is non-panel).}
}
\value{
Invisible character \code{"geocode_lineage"}.
}
\description{
Records one lineage row per write into \code{geocode_lineage}.
The input \code{x} may be an \code{alprek_geocode_clean},
\code{alprek_geocode_reconciled}, \code{alprek_geocode_master}, or
\code{alprek_geocode_panel}. For panels, one row per \code{geocode_run_id}
in \code{panel$binding_log} is appended.

Lineage columns:
\itemize{
\item \code{geocode_run_id}            (character)
\item \code{source}                    (character; e.g., \code{"melissa"})
\item \code{cycle_year}                (character)
\item \code{snapshot_date}             (character / ISO date)
\item \code{file_sha256}               (character)
\item \code{git_sha}                   (character)
\item \code{n_rows}                    (integer)
\item \code{n_followup}                (integer; counts when known)
\item \code{distance_threshold_rules}  (character)
\item \code{flat_threshold_m}          (integer)
\item \code{written_at}                (character timestamp)
}
}
