% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-transform.R
\name{geocode_transform}
\alias{geocode_transform}
\title{Transform Reconciled Geocode Data Into Master Object (Step 5.1)}
\usage{
geocode_transform(reconciled, config = NULL)
}
\arguments{
\item{reconciled}{An \code{alprek_geocode_reconciled} object from
\code{\link[=geocode_reconcile]{geocode_reconcile()}}.}

\item{config}{Optional \code{alprek_geocode_config} (from \code{\link[=geocode_config]{geocode_config()}}).
When \code{NULL}, a minimal default config is constructed from
\code{reconciled$meta} (\code{vendor = meta$source \%||\% "melissa"},
\code{delivery_date = meta$receipt_date}). Used to build \code{geocode_run_id}.}
}
\value{
An \code{alprek_geocode_master} S3 list with elements:
\itemize{
\item \code{data}: tibble of reconciled data + 5 new derived columns.
Preserves \code{lineage_id} and \code{coord_model_status} columns intact.
\item \code{transform_log}: tibble (one row per derivation rule) with columns
\code{rule}, \code{n_affected}, \code{details}, \code{severity} (one of
\code{"INFO"}/\code{"WARN"}/\code{"ERROR"}).
\item \code{meta}: list inheriting from \code{reconciled$meta} plus
\code{transformed_at} and \code{geocode_run_id}.
}
}
\description{
Adds 5 derived analytical variables on top of the reconciled
geocode output. Mirrors the \code{applications_transform()} / \code{budget_transform()}
pattern in this package: a thin, deterministic data-layer transform that
produces an \code{alprek_geocode_master} S3 object consumable by downstream
linkage, export, and modeling code.

\strong{Derived variables (in order):}
\enumerate{
\item \code{precision_tier} (ordered factor; rooftop > parcel > zip4 > zip5 >
centroid > area > unknown > none) - aliased from \code{lat_precision}
with descending-order levels for sorting. The underlying tier value
is unchanged; only the factor-level order is inverted so that
\code{sort(precision_tier)} puts highest-precision rows first.
\item \code{in_alabama} (logical; NA-able) - \code{lat_final} in \code{c(30, 36)} and
\code{lng_final} in \code{c(-89, -84)}. Both bounds inclusive. \code{NA} when
either \code{lat_final} or \code{lng_final} is \code{NA}.
\item \code{county_check_match} (logical; NA-able) - compares Melissa
\code{COUNTYNAME} against an \code{adece_county} sidecar (if present in the
reconciled \verb{$data}; e.g., G16 fixture). Returns \code{NA} for every row
in Step 5.1 standalone runs because the ADECE county column is not
part of the 29-column Melissa contract. Linkage diagnostics can use
this column when a county sidecar has been materialized; the default
v0.8.0 master join does not require it. Comparison is
case-insensitive (\code{tolower()} both sides).
\item \code{coord_age_years} (integer; NA-able) -
\code{cycle_year_first - school_year_first}. \code{cycle_year} and
\code{school_year} are both \code{"YYYY-YYYY"} (with optional \code{"_new"}
suffix on \code{school_year}). The first 4 characters of each are
coerced to integer; the suffix is ignored because \code{substr()} only
takes the leading year. Negative or implausible values are
preserved (analytical responsibility, not data-layer correction).
\item \code{geocode_run_id} (character) - panel-stable identifier built from
\code{config$vendor}, \code{"v1"}, and \code{format(config$delivery_date, "\%Y-\%m")}.
Default for the v0.8.0 single-snapshot path:
\code{"melissa_v1_2026-03"}. Every row in a single transform shares the
same value (the snapshot is one run); panels built across release
cycles (Step 5.2) carry distinct \code{geocode_run_id} values.
}

\strong{Phase 5 contract:} Exports must carry both
\code{coord_model_status} (from Step 4.3) and \code{lineage_id} (from Step 3.1).
Rows with \code{coord_model_status != "model_ready"} remain visible in
\verb{$data} and must NOT be silently promoted into downstream SAE-ready
master tables. This transform preserves both columns untouched.
}
\section{Implementation notes}{

\itemize{
\item \code{precision_tier} is an ordered factor with levels listed
\strong{descending} (highest precision first). This makes
\code{sort(precision_tier)} put rooftop rows first. The underlying
\code{lat_precision} column (ascending order, set by \code{geocode_reconcile()})
is left in place.
\item \code{county_check_match} reads an optional \code{adece_county} column that
test fixtures (e.g., G16) attach during the clean->reconcile pass.
Production callers should leave this column absent and rely on
Phase 6.1 linkage to materialize the comparison.
\item \code{geocode_run_id} matches the \code{geocode_run_id} token mentioned in
the Phase 5 plan book chapter (\code{06-phase5-transform-panel-export.qmd})
and in the future \code{geocode_bind_years()} (Step 5.2) panel scaffolding.
}
}

\examples{
\dontrun{
raw   <- geocode_read(path = "...", cycle_year = "2026-2027",
                      receipt_date = "2026-03-04")
clean <- geocode_clean(raw)
rec   <- geocode_reconcile(clean)
mst   <- geocode_transform(rec)
mst
}

}
\seealso{
\code{\link[=geocode_reconcile]{geocode_reconcile()}}, \code{\link[=geocode_config]{geocode_config()}}.
}
