% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-master.R
\name{.linkage_master_apply_geocode}
\alias{.linkage_master_apply_geocode}
\title{Apply the geocode-classroom linkage to an in-progress classroom_level tibble.}
\usage{
.linkage_master_apply_geocode(classroom_level, classroom_panel, geocode_panel)
}
\description{
Calls \code{\link[=linkage_geocode_classroom]{linkage_geocode_classroom()}} on the geocode panel + the original
classroom panel to obtain the prefixed \verb{geocode_*} columns, then attaches
the slim per-(site_code, school_year) lookup to \code{classroom_level} via a
left-join on \verb{(classroom_code, school_year)}. We join on classroom_code
rather than re-running the site-level join here so that we preserve
classroom_level's exact row order.
}
\details{
Returns the augmented tibble with an attached \code{.geocode_linkage_meta}
attribute carrying the diagnostics + meta from the underlying call.
}
\keyword{internal}
