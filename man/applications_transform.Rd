% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-transform.R
\name{applications_transform}
\alias{applications_transform}
\title{Transform Reconciled Applications Into Master Object}
\usage{
applications_transform(
  reconciled,
  capacity_clean = NULL,
  tier_bands = c(0, 2550, 3570, 4590, 5610)
)
}
\arguments{
\item{reconciled}{An \code{alprek_applications_reconciled} object.}

\item{capacity_clean}{Optional \code{alprek_applications_clean} with
\code{meta$kind == "capacity"}. If supplied, capacity-grain derivations are
added in \verb{$capacity_data}.}

\item{tier_bands}{Numeric vector of breakpoints (default
\code{c(0, 2550, 3570, 4590, 5610)}) - observed cycle-0 carry-forward dollar
amounts per tier. Used to infer \code{tier_prev_rank}.}
}
\value{
An \code{alprek_applications_master} S3 list with:
\itemize{
\item \code{data}: applications-grain tibble (reconciled + 7 derived cols)
\item \code{capacity_data}: capacity-grain tibble or NULL
\item \code{derived_log}: tibble (variable, formula, n_non_na, n_na, note)
\item \code{meta}: list inheriting from reconciled + \code{transformed_at},
\code{tier_bands}, \code{has_capacity}.
}
}
\description{
Adds simple, data-layer derived variables to a reconciled
applications object and (optionally) to a cleaned capacity object.
Mirrors the \code{budget_transform()} / \code{student_transform()} API.

\strong{In-scope derivations} (this function):
\itemize{
\item Applications grain: \code{is_renewal}, \code{is_new}, \code{is_oversubscribed_app}
(renewal had over-enrollment last cycle, if known),
\code{applied_this_cycle} (always \code{TRUE} per row; the column gains meaning
after \code{applications_bind_years()} or a join to \code{classroom_panel}),
\code{cycle_year} (carried from meta), \code{tier_prev_dollars},
\code{tier_prev_rank} (1-6 inferred from observed cycle-0 thresholds),
\code{tier_prev_band} (\code{"high"}/\code{"medium"}/\code{"low"}/NA).
\item Capacity grain (only if \code{capacity_clean} is provided):
\code{capacity_utilization = enrollment / capacity} (NA-safe),
\code{waitlist_ratio = waitlist / capacity} (NA-safe),
\code{is_oversubscribed} (waitlist > 0 OR enrollment > capacity).
}

\strong{Out-of-scope} (downstream packages - NOT computed here):
geocoded coordinates, ACS-weighted indicators, isochrone-derived
features, posterior tier from Bayesian SAE.
}
\examples{
\dontrun{
rec <- applications_reconcile(ren_clean, new_clean, panel)
mst <- applications_transform(rec, capacity_clean = cap_clean)
mst
}

}
