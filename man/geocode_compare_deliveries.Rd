% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-detect.R
\name{geocode_compare_deliveries}
\alias{geocode_compare_deliveries}
\title{Compare Two Melissa Geocode Deliveries}
\usage{
geocode_compare_deliveries(
  path_old,
  path_new,
  sheet = "Sheet1",
  rename_jw_threshold = 0.85,
  join_key = "row_id",
  enum_cols = c("school_year", "RESULTCODE", "STATUSCODE", "COUNTYNAME", "FIPS",
    "PLACENAME"),
  change_cols = c("site_code", "latitude", "longitude", "LAT", "LNG", "geocode_address",
    "site_street", "COUNTYNAME", "RESULTCODE"),
  verbose = FALSE
)
}
\arguments{
\item{path_old, path_new}{Character. Paths to the two delivery xlsx
files. Both must exist.}

\item{sheet}{Character. Sheet name to read in both files. Default
\code{"Sheet1"}. A requested sheet that is absent from either file
triggers \code{verdict = "breaking"}.}

\item{rename_jw_threshold}{Numeric in \verb{[0, 1]}. Default \code{0.85}.
Greedy 1-to-1 column-rename pairing requires Jaro-Winkler
similarity at least this large. Higher = stricter pairing.}

\item{join_key}{Character. Column to join on for row-level
comparison. Default \code{"row_id"}.}

\item{enum_cols}{Character vector. Columns whose distinct value sets
to inspect. Default
\code{c("school_year", "RESULTCODE", "STATUSCODE", "COUNTYNAME", "FIPS", "PLACENAME")}.}

\item{change_cols}{Character vector. Columns whose row-level values
to diff. Default
\code{c("site_code", "latitude", "longitude", "LAT", "LNG", "geocode_address", "site_street", "COUNTYNAME", "RESULTCODE")}.}

\item{verbose}{Logical. Print progress messages? Default \code{FALSE}.}
}
\value{
An \code{alprek_geocode_delivery_diff} S3 list with elements:
\itemize{
\item \verb{$meta} — list of input paths, SHA-256 hashes, sheet names,
dimensions for both deliveries, computed timestamp.
\item \verb{$schema_diff} — tibble (one row per column) with statuses
\code{"in_both"}, \code{"dtype_changed"}, \code{"possible_rename"}, \code{"added"},
\code{"removed"}.
\item \verb{$value_set_diff} — tibble (one row per \verb{(column, value)}) with
statuses \code{"in_both"}, \code{"added"}, \code{"removed"}.
\item \verb{$rows_only_old}, \verb{$rows_only_new} — tibbles of \code{row_id}s in
only one delivery, augmented with \code{likely_replaced_by} /
\code{likely_replaces} when a \verb{_new} ↔ resolved pair was found.
\item \verb{$rows_changed} — tibble of per-row, per-column changes
(NA-on-both = unchanged; NA-vs-value = changed).
\item \verb{$row_id_replaced_pairs} — tibble of \verb{_new} → assigned-site_code
row_id resolutions detected via
\verb{(school_year_root, site_name, geocode_address)}.
\item \verb{$summary} — tibble of \verb{(metric, value)} headline counters.
\item \verb{$verdict} — one of \code{"compatible"},
\code{"compatible_with_additions"}, \code{"breaking"}.
\item \verb{$verdict_reasons} — character vector of reason strings driving
the verdict.
}
}
\description{
Compares two Melissa-returned geocoded xlsx deliveries
end-to-end: schema (column set, dtypes, possible renames), enum
value sets, row-level changes (additions, removals, value
updates), and \verb{_new}-placeholder → assigned-site_code resolution
pairs. Returns a structured \code{alprek_geocode_delivery_diff} object
that the caller can inspect manually or hand to the printer for a
one-paragraph summary.

The verdict (\code{"compatible"}, \code{"compatible_with_additions"},
\code{"breaking"}) follows Step 1.4 of the format-diff protocol:
schema breaking changes, duplicate \code{row_id}s in either file, or
a sheet rename always escalate to \code{"breaking"}; otherwise the
delivery is \code{"compatible"} if neither value sets nor any rows
changed, else \code{"compatible_with_additions"}.
}
\examples{
\dontrun{
diff <- geocode_compare_deliveries(
  path_old = "ORIGINAL-DATA/2026-03-04_geocoding_master_Final.xlsx",
  path_new = "ORIGINAL-DATA/2026-09-15_geocoding_master_Final.xlsx"
)
diff
diff$verdict
diff$row_id_replaced_pairs
}

}
\seealso{
\code{\link[=geocode_detect_format]{geocode_detect_format()}}, \code{\link[=geocode_read]{geocode_read()}},
\code{\link[=alprek_geocode_column_map]{alprek_geocode_column_map()}}.
}
