% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-coverage.R
\name{linkage_coverage_geocode}
\alias{linkage_coverage_geocode}
\title{Summarize Geocode Coverage on a Classroom-Level Master}
\usage{
linkage_coverage_geocode(classroom_level)
}
\arguments{
\item{classroom_level}{A tibble (typically \code{master$classroom_level})
carrying the prefixed \verb{geocode_*} columns. May also be any data
frame; missing columns are treated as fully NA.}
}
\value{
A list with named scalar metrics:
\itemize{
\item \code{n_classroom_total}        -- total rows in \code{classroom_level}
\item \code{n_classroom_with_coord}   -- rows with non-NA \code{geocode_lat_final}
\item \code{n_needing_followup}       -- rows with \code{geocode_needs_followup_geocoding == TRUE}
\item \code{n_model_ready}            -- rows with \code{geocode_coord_model_status == "model_ready"}
\item \code{pct_with_coord}           -- 100 * (n_with_coord / n_total)
\item \code{pct_followup}             -- 100 * (n_needing_followup / n_total)
\item \code{pct_model_ready}          -- 100 * (n_model_ready / n_total)
\item \code{by_lat_source}            -- a \code{tibble} with \code{lat_source}, \code{n}, \code{pct}
(or NULL if column absent)
\item \code{by_coord_model_status}    -- a \code{tibble} with \code{coord_model_status},
\code{n}, \code{pct} (or NULL if column absent)
}
}
\description{
Builds a small coverage tibble that quantifies how many
classroom-year rows received an authoritative reconciled coordinate
from the geocode panel, how many still need follow-up, and how many
are model-ready for downstream Bayesian SAE consumers.

This helper is called by \code{\link[=linkage_create_master]{linkage_create_master()}} when a geocode
panel is supplied, and is also useful as a standalone diagnostic on
any tibble that has the prefixed \verb{geocode_*} columns introduced by
\code{\link[=linkage_geocode_classroom]{linkage_geocode_classroom()}}.

Recognized columns (all optional; the helper degrades gracefully):
\itemize{
\item \code{geocode_lat_final}             -- presence -> "has coord"
\item \code{geocode_needs_followup_geocoding} -- TRUE -> "needs followup"
\item \code{geocode_coord_model_status}    -- "model_ready" -> "model ready"
\item \code{geocode_lat_source}            -- factor levels rolled up
}
}
\keyword{internal}
