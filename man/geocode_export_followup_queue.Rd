% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-export.R
\name{geocode_export_followup_queue}
\alias{geocode_export_followup_queue}
\title{Export the Geocode Follow-Up Queue as a Production-Ready CSV}
\usage{
geocode_export_followup_queue(
  x,
  path = NULL,
  cycle_year = NULL,
  include_disputed = TRUE,
  internal_use = TRUE
)
}
\arguments{
\item{x}{An \code{alprek_geocode_reconciled} or \code{alprek_geocode_panel}.}

\item{path}{Character. Output path. If \code{NULL}, auto-generates
\verb{output/geocode/sites_needing_geocoding_<cycle_year>.csv}.
\verb{<cycle_year>} is taken from \code{cycle_year} (if supplied) or from
\code{x$meta$cycle_year}; falls back to \code{"unknown"} if neither resolves.}

\item{cycle_year}{Character or \code{NULL}. Override the auto-path's cycle
token (and the comment header). Default \code{NULL}.}

\item{include_disputed}{Logical. Forwarded to \code{\link[=geocode_followup_queue]{geocode_followup_queue()}}.
When \code{TRUE} (default), rows with \code{lat_source == "disputed_melissa"}
are retained in the queue.}

\item{internal_use}{Logical. When \code{TRUE} (default),
prepend an \verb{# INTERNAL USE -- DO NOT REDISTRIBUTE} header to the CSV
and set \code{attr(out, "internal_use") <- TRUE} on the returned tibble.}
}
\value{
Invisibly returns the in-memory follow-up queue tibble (with
privacy attributes attached). The CSV at \code{path} is the primary side
effect.
}
\description{
Writes the analyst-facing follow-up queue (sites needing
re-geocoding) to a CSV at a predictable path. This is the user-facing
surface for Goal #3 of v0.8.0: handing operations a named list of sites
to re-investigate after every Melissa delivery.

The function is a thin wrapper around \code{\link[=geocode_followup_queue]{geocode_followup_queue()}} from
\code{R/geocode-reconcile.R} that:
\itemize{
\item Accepts either an \code{alprek_geocode_reconciled} (preferred) or an
\code{alprek_geocode_panel} (multi-run; the panel is reduced to its
\verb{$data} and a synthetic reconciled-like shape is used to look up
follow-up flags directly).
\item Auto-generates the output path as
\verb{output/geocode/sites_needing_geocoding_<cycle_year>.csv} unless
\code{path} is supplied.
\item Prepends a clearly visible internal-use comment header to the CSV
when \code{internal_use = TRUE} (the default), per the package privacy
contract. The queue carries full site addresses and is NOT a public
deliverable.
\item Returns the in-memory tibble (invisibly) so callers can both write to
disk and inspect the queue in the same expression.
}

The exported CSV's columns match the Step 4.4 queue exactly (and start
with \code{lineage_id} for traceability):
\verb{lineage_id, row_id, school_year, site_code, site_name, site_street, site_city, site_state, site_zip, lat_source, coord_agreement_band, distance_adece_melissa_m, melissa_result_code, lat_precision, followup_reason, suggested_action}.

Per the follow-up action and privacy contract, the in-memory return
value also carries the attributes
\code{privacy_level = "internal_address_followup"} and
\code{contains_address_fields = TRUE}. When \code{internal_use = TRUE} the return
value additionally has \code{attr(., "internal_use") <- TRUE}.
}
\examples{
\dontrun{
raw   <- geocode_read(path = "...", cycle_year = "2026-2027")
rec   <- geocode_reconcile(geocode_clean(raw))
fq    <- geocode_export_followup_queue(rec)
nrow(fq)
attr(fq, "privacy_level")
}

}
\seealso{
\code{\link[=geocode_followup_queue]{geocode_followup_queue()}}, \code{\link[=geocode_reconcile]{geocode_reconcile()}},
\code{\link[=geocode_export_csv]{geocode_export_csv()}}.
}
