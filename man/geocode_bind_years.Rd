% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-panel.R
\name{geocode_bind_years}
\alias{geocode_bind_years}
\title{Bind Multiple Geocode Master Snapshots Into a Panel (Step 5.2)}
\usage{
geocode_bind_years(masters)
}
\arguments{
\item{masters}{A single \code{alprek_geocode_master} object OR a \code{list} of
them. Mixed-class lists are rejected.}
}
\value{
An \code{alprek_geocode_panel} S3 list with elements:
\itemize{
\item \code{data} — bound rows; for single-run input this is \code{masters$data}
verbatim (degenerate identity). For multi-run input, rows are
\code{dplyr::bind_rows}'d; the \code{geocode_run_id} column distinguishes
them.
\item \code{meta} — list with \code{n_runs}, \code{run_ids} (character),
\code{snapshot_dates} (Date vector), \code{snapshot_file_sha256s}
(character), \code{bound_at}, \code{n_rows_total}, \code{n_rows_per_run}
(named integer), \code{vendors} (character).
\item \code{binding_log} — per-run tibble: \code{geocode_run_id}, \code{snapshot_date},
\code{file_sha256}, \code{n_rows}, \code{n_columns}.
}
}
\description{
Stacks one or more \code{alprek_geocode_master} objects (each one
the output of a single Melissa delivery / vendor run) into a longitudinal
\code{alprek_geocode_panel}.

\strong{Important — what "panel" means here.} A single Melissa delivery is
\emph{already} a 5-year long panel (\code{school_year} ∈ \{2021-2022, ...,
2025-2026_new\}) — that within-delivery long shape is materialized
upstream of this function. \code{geocode_bind_years()} is for binding
\strong{multiple Melissa runs across release cycles} (e.g., a future v0.9.0
delivery on top of the current v0.8.0 delivery). For v0.8.0, only one
Melissa run exists, so the typical call collapses to a degenerate
identity (\code{geocode_bind_years(master_v1)} returns a 1-run panel whose
\verb{$data} is the input's \verb{$data}).

Each input master's \code{geocode_run_id} (built in
\code{\link[=geocode_transform]{geocode_transform()}} from \code{vendor_v1_YYYY-MM}) becomes the panel-row
discriminator. By design, the same \code{row_id} may appear in multiple runs
(a renewal site re-geocoded each release cycle); the unique key in the
bound panel is \verb{(row_id, geocode_run_id)}.
}
\section{Schema compatibility}{

Phase 5 contract: every master is expected to carry the same 29 + 10
"standard" geocode columns (29-col Melissa contract + 10 derived from
reconcile + transform). If runs differ on column membership (e.g., a
future delivery introduces a new RESULTCODE level or an extra Melissa
field), the function emits a \code{WARN} row in \verb{$binding_log} and still
binds via \code{dplyr::bind_rows} (which tolerates missing columns by
filling \code{NA}). It does NOT error.
}

\section{Key uniqueness}{

\verb{(row_id, geocode_run_id)} MUST be unique in the bound panel. If a run
contains internally-duplicated \code{row_id} values (a Phase 3 validator
should have caught this upstream), the function errors with a list of
the colliding keys.
}

\section{Phase 5 column preservation}{

\code{lineage_id} (stable row lineage from Step 3.1) and \code{coord_model_status}
(ordered factor from Step 4.3) are preserved in the bound panel exactly
as they appear in each input master. No silent promotion or dropping.
}

\examples{
\dontrun{
# Degenerate single-run panel (v0.8.0 typical use)
mst_v1 <- geocode_transform(geocode_reconcile(geocode_clean(geocode_read("..."))))
panel  <- geocode_bind_years(mst_v1)
panel

# Future multi-run use (v0.9.0+)
panel2 <- geocode_bind_years(list(mst_v1, mst_v2))
}

}
\seealso{
\code{\link[=geocode_transform]{geocode_transform()}}, \code{\link[=geocode_reconcile]{geocode_reconcile()}}.
}
