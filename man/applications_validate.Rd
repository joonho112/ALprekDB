% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-validate.R
\name{applications_validate}
\alias{applications_validate}
\title{Validate Cleaned or Reconciled Applications Data}
\usage{
applications_validate(x, strict = FALSE, tolerance = 1)
}
\arguments{
\item{x}{One of:
\itemize{
\item \code{alprek_applications_clean} (cleaned per-kind)
\item \code{alprek_applications_reconciled} (merged renewals + new_apps)
\item \code{alprek_applications_linkage} (classroom_panel x applications join)
}}

\item{strict}{Logical. If \code{TRUE}, treats warnings as overall failure.
Default \code{FALSE}.}

\item{tolerance}{Numeric. Dollar tolerance for cross-field reconciliation
checks. Default \code{1.00}.}
}
\value{
An \code{alprek_applications_validation} S3 list with elements:
\itemize{
\item \code{passed}: logical (overall result)
\item \code{n_errors}, \code{n_warnings}, \code{n_info}: integer counts
\item \code{kind}: e.g., \code{"renewals"}, \code{"new_apps"}, \code{"non_renewals"}, \code{"capacity"},
\code{"reconciled"}
\item \code{checks}: tibble of check results
\item \code{issues}: tibble of offending rows (one row per issue, with
\code{issue_type} plus key context columns)
}
}
\description{
Comprehensive data-quality checks on a single cleaned ADECE
applications object (\code{alprek_applications_clean}) or the merged
\code{alprek_applications_reconciled} object. Mirrors \code{budget_validate()} and
\code{classroom_validate()} API: each check is logged with a structured row
(\code{check_name}, \code{check_description}, \code{status} one of \code{PASS}, \code{ERROR}, \code{WARN}, \code{INFO},
\code{n_issues}, \code{details}), and offending rows accumulate in \verb{$issues}.

Validation is scoped to the \strong{data contract layer}: column existence,
value ranges, cross-field consistency, codebook membership, provenance.
Geocoding / ACS / Bayesian-modelling checks live in downstream packages.
}
\examples{
\dontrun{
clean <- applications_clean(applications_read_renewals(path, "2026-2027"))
v <- applications_validate(clean)
print(v)
v$checks
v$issues
}

}
