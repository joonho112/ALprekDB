% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-read.R
\name{db_read_panel}
\alias{db_read_panel}
\title{Read Panel Data from DuckDB}
\usage{
db_read_panel(conn, module = c("budget", "classroom", "student"), years = NULL)
}
\arguments{
\item{conn}{A DBI connection object from \code{\link[=db_init]{db_init()}}.}

\item{module}{Character. Which module to read: \code{"budget"}, \code{"classroom"},
or \code{"student"}.}

\item{years}{Character vector or \code{NULL}. School years to include (e.g.,
\code{c("2023-2024", "2024-2025")}). \code{NULL} reads all years.}
}
\value{
An \code{alprek_budget_panel}, \code{alprek_classroom_panel}, or
\code{alprek_student_panel} S3 object.
}
\description{
Reads panel data from the database and reconstructs the original
S3 object with proper R types (factors, Dates, integers). Optionally
filters by school year.
}
\examples{
\dontrun{
conn <- db_init("output/alprekdb.duckdb", read_only = TRUE)
budget <- db_read_panel(conn, "budget")
student <- db_read_panel(conn, "student", years = "2024-2025")
db_close(conn)
}

}
