% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/student-validate.R
\name{student_validate}
\alias{student_validate}
\title{Validate Cleaned Student Data}
\usage{
student_validate(clean_obj, strict = FALSE)
}
\arguments{
\item{clean_obj}{An \code{alprek_student_clean} object from \code{\link[=student_clean]{student_clean()}}.}

\item{strict}{Logical. If \code{TRUE}, treats warnings as errors. Default \code{FALSE}.}
}
\value{
An \code{alprek_student_validation} S3 object (list) with elements:
\itemize{
\item \code{passed}: logical overall result.
\item \code{n_errors}, \code{n_warnings}, \code{n_info}: counts by severity.
\item \code{checks}: tibble of individual check results.
\item \code{issues}: tibble of specific rows with problems.
}
}
\description{
Performs comprehensive data quality checks on cleaned student
data. Returns a validation report with pass/fail status for each check.
Validation is advisory — it does not block downstream processing.
}
\examples{
\dontrun{
raw <- student_read("FCPK Student Details 21-22.xlsx", "2021-2022")
clean <- student_clean(raw)
val <- student_validate(clean)
print(val)
}

}
