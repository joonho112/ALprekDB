% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-config.R
\name{applications_config}
\alias{applications_config}
\title{Create an Applications Processing Configuration}
\usage{
applications_config(
  cycle_year,
  master_path = NULL,
  renewals_path = NULL,
  new_apps_path = NULL,
  consolidated_path = NULL,
  prior_deliverable_path = NULL,
  output_dir = NULL,
  fuzzy_threshold = 0.85,
  cycle = c("cycle1", "cycle0"),
  seed = 20260519L,
  remove_noise_rows = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{cycle_year}{Character. Cycle year in \code{"YYYY-YYYY"} format (e.g.,
\code{"2026-2027"}). Required.}

\item{master_path}{Character. Path to the cycle-1 master xlsx file
containing all renewal/new/non-renewal/capacity sheets. Required for
cycle-1 (combined-file layout).}

\item{renewals_path}{Character. Path to cycle-0 renewals xlsx file
(separate-file layout). Used when cycle-0 is being re-processed.
Default \code{NULL}.}

\item{new_apps_path}{Character. Path to cycle-0 new applications xlsx file.
Default \code{NULL}.}

\item{consolidated_path}{Character. Path to "Classroom requests" or
equivalent supplementary xlsx file. Optional. Default \code{NULL}.}

\item{prior_deliverable_path}{Character. Path to prior cycle final
deliverable xlsx (e.g., 2025-2026 full application list_Added_Economic
Needs.xlsx) — used as reference / YoY comparison. Default \code{NULL}.}

\item{output_dir}{Character. Output directory. Default \code{NULL} (auto:
\verb{output/applications/<cycle_year>}).}

\item{fuzzy_threshold}{Numeric in (0, 1). Jaro-Winkler similarity threshold
for fuzzy classroom-name matching in \code{applications_reconcile()}. Default
\code{0.85}.}

\item{cycle}{Character. Cycle schema label ("cycle1" / "cycle0"). Used by
\code{applications_detect_format()} and codebook loaders. Default \code{"cycle1"}.}

\item{seed}{Integer. Random seed for reproducibility (used by fuzzy match
deterministic tiebreaks etc.). Default \code{20260519L}.}

\item{remove_noise_rows}{Logical. Drop noise rows (e.g., "Show the Debugger
Trace Report") at clean step? Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages? Default \code{TRUE}.}
}
\value{
An \code{alprek_applications_config} S3 object.
}
\description{
Creates a typed configuration object that controls the
applications module pipeline (read -> clean -> reconcile -> validate ->
transform -> panel -> export).
}
\examples{
\dontrun{
cfg <- applications_config(
  cycle_year = "2026-2027",
  master_path = file.path("ORIGINAL-DATA", "applications_2026_2027.xlsx")
)
result <- applications_process(cfg)
}

}
