% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-clean.R
\name{geocode_clean}
\alias{geocode_clean}
\title{Clean Melissa-Returned Geocoded Master Data}
\usage{
geocode_clean(raw, config = NULL)
}
\arguments{
\item{raw}{An \code{alprek_geocode_raw} object from \code{\link[=geocode_read]{geocode_read()}}.}

\item{config}{Optional \code{alprek_geocode_config} (from
\code{\link[=geocode_config]{geocode_config()}}). If \code{NULL}, a minimal default is constructed
from \code{raw$meta}.}
}
\value{
An \code{alprek_geocode_clean} S3 object (list) with elements:
\itemize{
\item \code{data}: tibble of cleaned data; preserves source columns plus
\code{raw_row_index} and \code{lineage_id}. \code{data_source_map} attribute names
each column by provenance group.
\item \code{cleaning_log}: tibble with columns \code{rule}, \code{n_affected},
\code{details}, \code{severity} (one of \code{INFO}/\code{WARN}/\code{ERROR}).
\item \code{meta}: list inheriting key provenance from \code{raw$meta}
(\code{file_sha256}, \code{git_sha}, \code{source}, \code{cycle_year},
\code{receipt_date}, \code{path}, \code{sheet}, \code{file_basename}) plus
\code{geocoding_source = "melissa_v1_2026"}, row-index mirrors,
\code{n_rows}, and \code{cleaned_at}.
}
}
\description{
Standardizes column names, dtypes, and value formatting of
an \code{alprek_geocode_raw} object so downstream geocoding steps
(\code{geocode_validate()}, \code{geocode_reconcile()}) can operate against a
stable contract. The 11-step pipeline (in order) is:
\enumerate{
\item Apply column map (rename if needed per
\code{alprek_geocode_column_map()}; for v1 most names are unchanged).
\item Coerce Melissa \code{LAT} / \code{LNG} from character to numeric. This is
the key transformation. Coercion failures are logged.
\item Defensively coerce ADECE \code{latitude} / \code{longitude} to numeric.
\item Coerce \code{ERRORCODE} from logical to character. (\code{readxl} parses
all-NA columns as logical — standardize to character so future
deliveries that populate ERRORCODE compose with the same schema.)
\item Keep ZIP-family fields character. \code{site_zip} is parsed as
numeric by \code{readxl} from a numeric column; convert to character
to preserve leading zeros if any. \code{GEOZIP} / \code{PLUS4} / \code{DPB} are
already character but trimmed defensively.
\item Standardize \code{school_year}: trim, validate against canonical set
\verb{\{2021-2022, 2022-2023, 2023-2024, 2024-2025, 2025-2026_new\}}.
Unknown values are logged with severity \code{WARN}.
\item Title-case \code{COUNTYNAME} (Melissa returns ALL-CAPS).
Cross-validate against \code{alprek_geocode_al_fips_counties()};
unrecognized AL county names are logged with severity \code{WARN}.
\item Trim whitespace on all character columns.
\item Defensively coerce \code{has_latlon} to logical.
\item Attach \code{data_source_map} attribute that labels each column by
provenance group: id/adece -> "ADECE", melissa_norm/melissa_out
-> "Melissa-\if{html}{\out{<delivery>}}".
\item Drop lock-file artifact rows if any \verb{~$*.xlsx} rows leaked into
the data (defensive).
}

\code{raw_row_index} and \code{lineage_id} are preserved unchanged.
}
\examples{
\dontrun{
raw   <- geocode_read(path = "...", cycle_year = "2026-2027")
clean <- geocode_clean(raw)
clean
}

}
\seealso{
\code{\link[=geocode_read]{geocode_read()}}, \code{\link[=alprek_geocode_column_map]{alprek_geocode_column_map()}},
\code{\link[=alprek_geocode_al_fips_counties]{alprek_geocode_al_fips_counties()}}.
}
