% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-diagnostics.R
\name{.linkage_validate_geocode_checks}
\alias{.linkage_validate_geocode_checks}
\title{Build the geocode-specific check rows for \code{linkage_validate()}.}
\usage{
.linkage_validate_geocode_checks(
  linkage_obj,
  coverage_min = 0.95,
  agreement_min = 0.95,
  model_ready_min = 0.7
)
}
\description{
Returns a (possibly empty) list of \code{.make_check()}-shaped objects. The
checks gracefully no-op when the input is not an
\code{alprek_linkage_master}, when its \code{classroom_level} has no \verb{geocode_*}
columns (3-arg master path), or when an individual check's required
column(s) are absent on the master.
}
\details{
The five checks (each produces 0 or 1 result row):
\enumerate{
\item \code{geocode_coverage_classroom}   -- WARN if non-NA \code{geocode_lat_final}
coverage < \code{coverage_min}.
\item \code{followup_reason_completeness} -- ERROR if any row with
\code{geocode_needs_followup_geocoding == TRUE}
has NA \code{geocode_followup_reason}.
\item \code{county_check_agreement}       -- WARN if Melissa-vs-classroom county
agreement rate < \code{agreement_min}.
Skipped if no county comparison
column is materialized on the
master.
\item \code{new_site_followup_visibility} -- INFO surfacing the count of
bucket-D applications needing
followup geocoding. Skipped if
no applications branch fired.
\item \code{model_ready_threshold}        -- WARN if \code{pct_model_ready} (from
\code{diagnostics$geocode_coverage}) is
below \code{model_ready_min}.
}
}
\keyword{internal}
