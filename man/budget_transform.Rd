% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/budget-transform.R
\name{budget_transform}
\alias{budget_transform}
\title{Transform Budget Data to Wide Format with Derived Variables}
\usage{
budget_transform(
  budget_long_obj,
  add_groups = TRUE,
  add_shares = TRUE,
  allocate_payroll = TRUE
)
}
\arguments{
\item{budget_long_obj}{An \code{alprek_budget_long} object from \code{\link[=budget_clean]{budget_clean()}}.}

\item{add_groups}{Logical. Add delivery type groupings (binary and 3-way)?
Default is \code{TRUE}.}

\item{add_shares}{Logical. Add percentage share columns? Default is \code{TRUE}.}

\item{allocate_payroll}{Logical. Allocate payroll taxes proportionally to
lead/aux teacher benefits? Applies only to legacy format. Default is \code{TRUE}.}
}
\value{
An \code{alprek_budget_master} S3 object (list) with elements:
\itemize{
\item \code{data}: tibble in wide format, one row per classroom-year.
\item \code{meta}: list of metadata.
}
}
\description{
Transforms long-format budget data into a wide, analysis-ready
master dataset with one row per classroom-year. Adds derived variables
including category totals, percentage shares, and delivery type groupings.
}
\examples{
\dontrun{
raw <- budget_read("rptClassBudgets 2023-2024.xlsx", "2023-2024")
cleaned <- budget_clean(raw)
master <- budget_transform(cleaned)
master$data
}

}
