% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-applications.R
\name{db_write_applications_master}
\alias{db_write_applications_master}
\title{Write Applications Master to DuckDB}
\usage{
db_write_applications_master(conn, master, overwrite = FALSE)
}
\arguments{
\item{conn}{A DBI connection (from \code{db_init()}).}

\item{master}{An \code{alprek_applications_master}.}

\item{overwrite}{Logical. If \code{TRUE}, drop and recreate the tables before
writing. Default \code{FALSE} - duplicates \code{(cycle_year)} rows are rejected.}
}
\value{
Invisible character vector of tables written.
}
\description{
Persists an \code{alprek_applications_master} into DuckDB across
up to four tables:
\itemize{
\item \code{applications_clean} - per-cycle, applications-grain rows
(\code{master$data}).
\item \code{applications_capacity} - per-cycle, capacity-grain rows
(\code{master$capacity_data}); only created when \code{master$capacity_data}
is non-NULL.
\item \code{applications_lineage} - one row per write, capturing
\code{cycle_year}, \code{file_sha256}, \code{git_sha}, \code{reconciled_at},
\code{transformed_at}, and \code{written_at} for downstream traceability.
\item \code{applications_derived_log} - per-cycle derivation audit rows from
\code{master$derived_log}.
}

Type registry is updated through the shared
\code{.db_register_column_types()} helper so reads reconstruct factor /
integer / numeric columns correctly.
}
