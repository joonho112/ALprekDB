% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-reconcile.R
\name{geocode_followup_queue}
\alias{geocode_followup_queue}
\title{Build a Follow-Up Queue From a Reconciled Geocode Object (Step 4.4)}
\usage{
geocode_followup_queue(reconciled, include_disputed = TRUE)
}
\arguments{
\item{reconciled}{An \code{alprek_geocode_reconciled} object from
\code{\link[=geocode_reconcile]{geocode_reconcile()}}.}

\item{include_disputed}{Logical. When \code{TRUE} (default), rows whose
\code{lat_source == "disputed_melissa"} (matrix cells D3 / D6 / D9 / D10)
are included in the queue. Set to \code{FALSE} to exclude them (e.g., when
the analyst handles disputed rows in a separate workflow).}
}
\value{
A tibble (not S3) with one row per site needing follow-up, ordered
per the sort rules above. Columns, in order:
\itemize{
\item \code{lineage_id} (character; stable row-level lineage key)
\item \code{row_id} (character)
\item \code{school_year} (character)
\item \code{site_code} (character)
\item \code{site_name} (character)
\item \code{site_street}, \code{site_city}, \code{site_state}, \code{site_zip} (character)
\item \code{lat_source} (character; coerced from the reconciled factor)
\item \code{coord_agreement_band} (character)
\item \code{distance_adece_melissa_m} (numeric; NA where not computable)
\item \code{melissa_result_code} (character; renamed from \code{RESULTCODE})
\item \code{lat_precision} (character)
\item \code{followup_reason} (character)
\item \code{suggested_action} (character; one of the controlled vocab values)
}

Returns a 0-row tibble with the same schema when no rows need follow-up
(or when all flagged rows are disputed and \code{include_disputed = FALSE}).
All returned queues, including 0-row outputs, carry attributes
\code{privacy_level = "internal_address_followup"} and
\code{contains_address_fields = TRUE}.
}
\description{
Surfaces the subset of reconciled site-rows that need analyst
follow-up geocoding, sorted for triage and annotated with a suggested
action. This is the consumer-facing helper that turns the per-row
\code{needs_followup_geocoding} flag (from \code{\link[=geocode_reconcile]{geocode_reconcile()}}) into a
short, actionable queue.

Each row in the output represents one site/year that the reconciler
flagged for follow-up. The \code{suggested_action} column is a controlled
vocabulary derived deterministically from \code{followup_reason}, so the
downstream caller can group/filter the queue without re-reading the
decision matrix.

Sort order (descending priority):
\enumerate{
\item \code{school_year} descending (latest year first).
\item \code{distance_adece_melissa_m} descending (largest disagreement
first); rows with \code{NA} distance (one-source-only or both-missing)
sort to the end.
}

\code{suggested_action} mapping (controlled vocabulary):
\tabular{ll}{
\code{followup_reason}              \tab \code{suggested_action} \cr
\code{disagreement_above_threshold} \tab \code{manual_source_adjudication} \cr
\code{disagreement_gross}           \tab \code{verify_adece_address_and_request_recheck} \cr
\code{resultcode_not_acceptable_for_master} \tab \code{request_higher_precision_geocode} \cr
\code{both_missing}                 \tab \code{obtain_coord} \cr
\code{melissa_unexpectedly_missing} \tab \code{request_melissa_geocode} \cr
\code{melissa_only_interpolated}    \tab \code{request_higher_precision_geocode} \cr
\code{melissa_only_gs03}            \tab \code{request_higher_precision_geocode} \cr
\code{resultcode_gs03_always_flag}  \tab \code{manual_review_gs03} \cr
other / \code{NA}                   \tab \code{manual_review} \cr
}
}
\examples{
\dontrun{
raw   <- geocode_read(path = "...", cycle_year = "2026-2027",
                      receipt_date = "2026-03-04")
clean <- geocode_clean(raw)
rec   <- geocode_reconcile(clean)
fq    <- geocode_followup_queue(rec)
table(fq$suggested_action)
# Exclude disputed Melissa rows (handled separately):
fq_no_disputed <- geocode_followup_queue(rec, include_disputed = FALSE)
}

}
\seealso{
\code{\link[=geocode_reconcile]{geocode_reconcile()}} for the upstream decision matrix that
produces \code{needs_followup_geocoding} and \code{followup_reason}.
}
