% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-geocode.R
\name{db_read_geocode_reconciled}
\alias{db_read_geocode_reconciled}
\title{Read a reconciled geocode object back from DuckDB}
\usage{
db_read_geocode_reconciled(conn, run_id = NULL)
}
\arguments{
\item{conn}{A DBI connection from \code{\link[=db_init]{db_init()}}.}

\item{run_id}{Optional character. When \code{NULL}, the most-recent run.}
}
\value{
An \code{alprek_geocode_reconciled} object.
}
\description{
Reconstructs an \code{alprek_geocode_reconciled} from the
\code{geocode_reconciled} table. Ordered factor levels for \code{lat_precision}
and \code{coord_model_status} round-trip via the column type registry.
The \code{reconciliation_log} is reconstructed as an empty placeholder
(the original log is not persisted in v0.8.0; downstream consumers
that need it should call \code{\link[=geocode_reconcile]{geocode_reconcile()}} again).
}
