% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-clean.R
\name{applications_clean}
\alias{applications_clean}
\title{Clean ADECE Applications Data}
\usage{
applications_clean(raw, cycle = NULL, remove_noise_rows = TRUE)
}
\arguments{
\item{raw}{An \code{alprek_applications_raw} object (from \verb{applications_read_*()}).}

\item{cycle}{Character. Cycle schema label. Default auto-detected via
\code{\link[=applications_detect_format]{applications_detect_format()}}.}

\item{remove_noise_rows}{Logical. Drop rows whose \code{process_name} maps to
\code{kind_inferred == "noise"} in \code{applications_status_codes.csv}? Default
\code{TRUE}.}
}
\value{
An \code{alprek_applications_clean} S3 object with elements:
\itemize{
\item \code{data}: tibble of cleaned data with standardized column names and row
lineage fields
\item \code{cleaning_log}: tibble of changes applied (variable, rule, n_rows),
including parse failures and aggregate-row drops
\item \code{meta}: list inheriting from raw + cycle, n_rows_in, n_rows_out,
n_rows_dropped, cleaned_at
}
}
\description{
Standardizes column names and types of an
\code{alprek_applications_raw} object using cycle-specific column mappings from
\verb{inst/extdata/mappings/applications_column_map_<kind>_<cycle>.csv}. Filters
out known noise rows (e.g., "Show the Debugger Trace Report"), drops
capacity-sheet aggregate rows that have no \code{site_code}, and preserves
per-row \code{raw_row_index}/\code{lineage_id} plus \code{data_source} for provenance.

No geocoding, address parsing, or spatial work happens here — those are
handled by downstream packages.
}
\examples{
\dontrun{
raw <- applications_read_renewals(path, cycle_year = "2026-2027")
clean <- applications_clean(raw)
clean
}

}
