% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-reconcile.R
\name{applications_reconcile}
\alias{applications_reconcile}
\title{Reconcile ADECE Applications Against Prior Classroom Panel}
\usage{
applications_reconcile(
  renewals_clean,
  new_apps_clean,
  prior_classroom_panel = NULL,
  prior_school_year = NULL,
  fuzzy_threshold = 0.85,
  seed = 20260519L,
  allow_degraded = FALSE
)
}
\arguments{
\item{renewals_clean}{An \code{alprek_applications_clean} object whose
\verb{$meta$kind} is \code{"renewals"}.}

\item{new_apps_clean}{An \code{alprek_applications_clean} object whose
\verb{$meta$kind} is \code{"new_apps"}.}

\item{prior_classroom_panel}{Optional \code{alprek_classroom_panel} produced by
\code{classroom_panel()}. Required by default. When \code{NULL} and
\code{allow_degraded = TRUE}, no fuzzy work is performed and all rows receive
\code{bucket = "unknown"} so production workflows cannot confuse missing panel
data with exact reconciliation.}

\item{prior_school_year}{Optional character (e.g., \code{"2024-2025"}). The
school year in \code{prior_classroom_panel} to match against. When \code{NULL},
defaults to \code{max(prior_classroom_panel$years)}.}

\item{fuzzy_threshold}{Numeric in \verb{[0, 1]}. Similarity at or above which a
fuzzy candidate is auto-accepted. Default \code{0.85}.}

\item{seed}{Integer used for deterministic tie-breaking and recorded in the
audit log. Default \code{20260519L}.}

\item{allow_degraded}{Logical. If \code{TRUE}, allow \code{prior_classroom_panel = NULL}
for synthetic demos and return \code{bucket = "unknown"}. Default \code{FALSE} so
production workflows cannot mistake missing reconciliation for an exact
match.}
}
\value{
An \code{alprek_applications_reconciled} S3 list with elements:
\itemize{
\item \code{reconciled}: tibble with one row per input row, augmented with
\code{application_id}, \code{source_sheet}, \code{bucket}, \code{matched_classroom_code},
\code{matched_site_code}, \code{match_method}, \code{match_score}.
\item \code{reconciliation_log}: long tibble. One row per chosen decision plus up
to three runner-up \code{fuzzy_candidate} rows per non-exact decision.
Columns: \verb{application_id, source_sheet, name_raw, name_matched, match_method, score, threshold_used, decision_source, decision_timestamp, decision_seed, candidate_classroom_code, candidate_site_code, candidate_rank, score_margin, note}.
\item \code{summary}: tibble of bucket counts.
\item \code{meta}: list (\code{fuzzy_threshold}, \code{seed}, \code{reconciled_at},
\code{prior_school_year}, \code{n_in_renewals}, \code{n_in_new_apps}, \code{n_a}, \code{n_b},
\code{n_c}, \code{n_d}, \code{n_unknown}, \code{git_sha}).
}
}
\description{
Assigns each cycle-1 application row (renewals + new) into one
of four buckets and records every match decision (exact, fuzzy automatic,
no match) plus the top-3 fuzzy candidates considered. Solves Gap #1 from
the 2026-05 ad-hoc cycle (automatic match decisions weren't logged).

Buckets:
\itemize{
\item \strong{A} - renewal row, exact key match against \code{prior_classroom_panel}
(organization, prior project name, county). Carries the matched
\code{classroom_code}.
\item \strong{B} - renewal row, no exact match, fuzzy (Jaro-Winkler) similarity
\verb{>= fuzzy_threshold}. Carries best-match \code{classroom_code}, flagged for
analyst review.
\item \strong{C} - new-application row whose fuzzy similarity to a prior classroom
\verb{>= fuzzy_threshold} (probably an additional classroom at an existing
program). Carries best-match \code{classroom_code}, flagged.
\item \strong{D} - no candidate \verb{>= fuzzy_threshold}. Treated as truly new;
downstream geocoding + isochrone packages take it from here.
}

No geocoding, ACS integration, or Bayesian modelling here - those live in
separate packages.
}
\examples{
\dontrun{
r <- applications_read_renewals(path, cycle_year = "2026-2027")
n <- applications_read_new(path, cycle_year = "2026-2027")
rc <- applications_clean(r); nc <- applications_clean(n)
rec <- applications_reconcile(rc, nc, prior_classroom_panel = panel)
rec
rec$summary
}

}
