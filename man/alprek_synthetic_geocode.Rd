% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-synthetic-geocode.R
\name{alprek_synthetic_geocode}
\alias{alprek_synthetic_geocode}
\title{Generate Synthetic Geocoded Master Data}
\usage{
alprek_synthetic_geocode(
  n_sites = 50L,
  n_years = 3L,
  share_missing_adece = 0.1,
  share_missing_site_code = 0.03,
  share_high_resultcode_agreement = 0.7,
  edge_case = NULL,
  cycle_year_anchor = 2024L,
  seed = 20260520L
)
}
\arguments{
\item{n_sites}{Integer. Number of distinct sites (renewal pattern).
Default \code{50L}. Sites are stable across years (same \code{row_id},
\code{site_code}, \code{site_name} repeated).}

\item{n_years}{Integer. Number of school years to span. Default \code{3L}.
Most-recent year is \code{cycle_year_anchor} and the panel extends
backward. Total rows = \code{n_sites * n_years} (plus a small "_new"
cohort if \code{n_years >= 4}).}

\item{share_missing_adece}{Numeric between 0 and 1, inclusive. Share
of rows where the ADECE \code{latitude}/\code{longitude} is \code{NA} (and
\code{has_latlon} is \code{FALSE}). Default \code{0.10} (deliberately higher than
the v0.8.0 empirical ~5.4\% so the synthetic signal is unambiguous in
tests).}

\item{share_missing_site_code}{Numeric between 0 and 1, inclusive.
Share of rows where \code{site_code} is \code{NA} (the \verb{_new} cohort pattern).
Default \code{0.03}.}

\item{share_high_resultcode_agreement}{Numeric between 0 and 1,
inclusive. Share of rows assigned \code{RESULTCODE == "GS05"} (rooftop,
high agreement quality). The remainder split across \code{c("GS06", "GS03", "GS01")} at the v0.8.0 empirical ratios. Default \code{0.7}.}

\item{edge_case}{Character or NULL. If non-NULL, must be one of
\code{"G01"}..\code{"G18"}; the function returns the package-internal
fixture's \verb{$data} tibble. Default \code{NULL} (generate full synthetic
panel).}

\item{cycle_year_anchor}{Integer. Anchor year (Y for the
\code{"YYYY-YYYY+1"} school_year of the most recent observation). The
synthetic panel extends backward by \code{n_years - 1} years from this
anchor. Default \code{2024L} (so anchor school_year is \code{"2024-2025"}).}

\item{seed}{Integer. Random seed for reproducibility. Default
\code{20260520L}.}
}
\value{
A tibble with 29 columns matching the Melissa v1 column map
(see \code{alprek_geocode_column_map()}):
\itemize{
\item 5 id cols: row_id, school_year, site_name, site_code,
geocode_address
\item 7 adece cols: site_street, site_city, site_state, site_zip,
latitude, longitude, has_latlon
\item 6 melissa_norm cols: md_street, md_city, md_state, GEOZIP, PLUS4,
DPB
\item 11 melissa_out cols: LAT, LNG, CT, CENSUSBLOC, FIPS, COUNTYNAME,
PLACENAME, PLACECODE, RESULTCODE, STATUSCODE, ERRORCODE
}

Notable dtype contracts (v0.8.0):
\itemize{
\item \code{LAT} and \code{LNG} are CHARACTER (Melissa source contract; coerced
to numeric only in \code{geocode_clean()}).
\item \code{ERRORCODE} is LOGICAL and 100\% \code{NA} (v0.8.0 contract).
\item \code{latitude} / \code{longitude} are NUMERIC (ADECE source).
}
}
\description{
Creates a synthetic geocoded master tibble matching the
29-column Melissa v1 delivery contract. Mirrors the empirical
distributions observed in the v0.8.0 audit (school_year, RESULTCODE,
ADECE-coord missing patterns, AL geography) but uses fake site_codes
(\verb{999P}-prefix) and synthetic anchors so that examples cannot be
confused with confidential ADECE source records.

Designed for vignette, tests, and demonstrations. Returns a flat
tibble (not an S3 panel object) so callers can hand it directly into
\code{geocode_clean()}, \code{geocode_validate()}, and \code{geocode_reconcile()}.

When \code{edge_case} is non-NULL (one of \code{"G01"}-\code{"G18"}), returns a
package-internal 5-row fixture with one row deliberately mutated to
trigger that edge case. Useful for golden tests of \code{geocode_validate()}
/ \code{geocode_reconcile()} behavior.
}
\examples{
# Default invocation: 50 sites x 3 years ~ 150 rows
g <- alprek_synthetic_geocode()
nrow(g)
ncol(g)

# Smaller panel for tests
g_small <- alprek_synthetic_geocode(n_sites = 10, n_years = 2,
                                      seed = 42)
nrow(g_small)

# Edge-case mini-fixture (G05: drift; ADECE-Melissa distance 1-10km)
g_g05 <- alprek_synthetic_geocode(edge_case = "G05")
nrow(g_g05)  # 5 rows; row 1 has the drift mutation

}
