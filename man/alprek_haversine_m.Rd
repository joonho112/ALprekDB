% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-geo.R
\name{alprek_haversine_m}
\alias{alprek_haversine_m}
\title{Haversine Great-Circle Distance in Meters}
\usage{
alprek_haversine_m(lat1, lon1, lat2, lon2)
}
\arguments{
\item{lat1}{Numeric vector. Latitude of point 1, in decimal degrees.}

\item{lon1}{Numeric vector. Longitude of point 1, in decimal degrees.}

\item{lat2}{Numeric vector. Latitude of point 2, in decimal degrees.}

\item{lon2}{Numeric vector. Longitude of point 2, in decimal degrees.}
}
\value{
Numeric vector of distances in meters, same length as the inputs.
}
\description{
Vectorized great-circle distance between pairs of points on the surface of
a sphere using the haversine formula. Uses mean Earth radius 6,371,000 m.

If the \code{geosphere} package is available, the function delegates to
\code{\link[geosphere:distHaversine]{geosphere::distHaversine()}} as a sanity-checked implementation (geosphere
uses a slightly different mean radius and a numerically stable form);
otherwise it falls back to a vendored haversine that uses
\code{2 * R * asin(min(1, sqrt(a)))} to remain numerically stable near
antipodal points.
}
\details{
All four arguments must have the same length. \code{NA} inputs propagate to
\code{NA} outputs. The function does not validate that latitudes are in
\verb{[-90, 90]} or longitudes in \verb{[-180, 180]}; callers should pre-validate
if strict bounds are required.
}
\examples{
# Birmingham, AL <-> Mobile, AL is ~320 km
alprek_haversine_m(33.5186, -86.8104, 30.6954, -88.0399)

# Vectorized over multiple pairs
alprek_haversine_m(
  lat1 = c(33.5186, 33.2098),
  lon1 = c(-86.8104, -87.5692),
  lat2 = c(30.6954, 33.5186),
  lon2 = c(-88.0399, -86.8104)
)

}
