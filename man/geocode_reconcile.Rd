% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-reconcile.R
\name{geocode_reconcile}
\alias{geocode_reconcile}
\title{Reconcile ADECE and Melissa Geocoded Coordinates (Step 4.3)}
\usage{
geocode_reconcile(clean, config = NULL)
}
\arguments{
\item{clean}{An \code{alprek_geocode_clean} object from \code{\link[=geocode_clean]{geocode_clean()}}.
Must contain \code{latitude}, \code{longitude} (ADECE), \code{LAT}, \code{LNG} (Melissa),
\code{RESULTCODE}, and an identifier column (\code{row_id} preferred,
\code{raw_row_index} as fallback).}

\item{config}{Optional \code{alprek_geocode_config} (from \code{\link[=geocode_config]{geocode_config()}}).
When \code{NULL}, the reconciler uses the default LOCKED matrix:
\code{authoritative_priority = "melissa_first"},
\code{distance_threshold_rules = "by_resultcode"},
\code{tiered_thresholds = list(GS01 = 50, GS05 = 250, GS06 = 500, GS03 = Inf)}.}
}
\value{
An \code{alprek_geocode_reconciled} S3 list with elements:
\itemize{
\item \code{data}: tibble (1 row per input row) with all original columns plus 10
new authoritative columns:
\itemize{
\item \code{lat_final}, \code{lng_final} (numeric or NA)
\item \code{lat_source} (factor:
\verb{\{melissa, adece, disputed_melissa, none\}})
\item \code{lat_precision} (ordered factor:
\verb{\{none, unknown, centroid, zip5, zip4, area, parcel, rooftop\}},
increasing precision)
\item \code{distance_adece_melissa_m} (numeric or NA)
\item \code{coord_agreement_band} (factor:
\verb{\{exact, tight, loose, drift, gross, one_source_only, none\}})
\item \code{needs_followup_geocoding} (logical)
\item \code{followup_reason} (factor with controlled vocabulary)
\item \code{coord_model_status} (ordered factor:
\verb{\{missing, not_model_ready, provisional_followup, model_ready\}})
\item \code{geocode_provenance} (compact character string)
}
\item \code{reconciliation_log}: tibble (1 row per input row) of per-row audit
records (\code{row_id}, \code{matrix_cell}, \code{decision_source}, etc.).
\item \code{summary}: tibble of decision-cell counts.
\item \code{meta}: list (\code{reconciled_at}, \code{authoritative_priority},
\code{distance_threshold_rules}, \code{tiered_thresholds}, \code{n_rows},
\code{n_needs_followup}, \code{n_disputed}, \verb{n_lat_source_*}, \code{git_sha},
inherited from \code{clean$meta}).
}
}
\description{
Applies the LOCKED 15-cell decision matrix from Step 4.2 to a
cleaned Melissa-returned geocoded dataset. For each site row, picks an
authoritative (lat, lng) pair, records the precision tier, computes the
ADECE<->Melissa distance (when both sources are present), assigns a
coordinate-agreement band, and flags rows that need analyst followup.
Every decision is logged in \code{reconciliation_log} with the matrix cell ID
so downstream consumers (Step 4.4 followup queue, Step 4.5 sanity tests)
can audit any single-row decision without re-running the reconciler.

Decision-matrix scope (Step 4.2 LOCKED):
\itemize{
\item \strong{D1-D9} -- both sources present; outcome driven by RESULTCODE
(\code{GS01}/\code{GS05}/\code{GS06}), per-tier threshold, and gross-outlier check
(>=10 km).
\item \strong{D10} -- both sources present, RESULTCODE == \code{GS03}, any distance:
\code{disputed_melissa} (centroid is unreliable; flag for followup).
\item \strong{D11} -- ADECE only (Melissa unexpectedly missing): use ADECE, flag.
\item \strong{D12} -- Melissa only (\code{GS01}/\code{GS05}): use Melissa, OK (Melissa is
the authoritative geocode source for these rows).
\item \strong{D13} -- Melissa only (\code{GS06}, interpolated rooftop): use Melissa,
flag for analyst review.
\item \strong{D14} -- Melissa only (\code{GS03}, ZIP centroid): use Melissa, flag.
\item \strong{D15} -- both missing: no coordinate; flag with \code{both_missing}.
}

The matrix can be inverted at the priority level via
\code{config$authoritative_priority == "adece_first"}, in which case D1-D9
resolve to ADECE-anchored decisions (still flagged when distances exceed
the per-tier threshold).

Tier thresholds come from \code{config$tiered_thresholds} (default
\code{list(GS01 = 50, GS05 = 250, GS06 = 500, GS03 = Inf)}). The reconciler
never hardcodes RESULTCODE -> precision_tier; it looks them up from
\code{\link[=alprek_geocode_resultcode_meaning]{alprek_geocode_resultcode_meaning()}} so future codebook updates flow
through without code changes. \code{acceptable_for_master} in that same
codebook is also enforced: unacceptable or unknown RESULTCODE values are
retained for analyst review but flagged and marked \code{not_model_ready}.
}
\section{Decision matrix (15 cells)}{

\tabular{llllll}{
Cell \tab adece \tab melissa \tab RESULTCODE \tab dist vs tier \tab outcome \cr
D1   \tab Y     \tab Y       \tab GS01       \tab <=50m         \tab melissa, zip4, no followup \cr
D2   \tab Y     \tab Y       \tab GS01       \tab 50m..10km     \tab melissa, zip4, flag (disagreement_above_threshold) \cr
D3   \tab Y     \tab Y       \tab GS01       \tab >=10km        \tab disputed_melissa, zip4, flag (disagreement_gross) \cr
D4   \tab Y     \tab Y       \tab GS05       \tab <=250m        \tab melissa, rooftop, no followup \cr
D5   \tab Y     \tab Y       \tab GS05       \tab 250m..10km    \tab melissa, rooftop, flag \cr
D6   \tab Y     \tab Y       \tab GS05       \tab >=10km        \tab disputed_melissa, rooftop, flag (gross) \cr
D7   \tab Y     \tab Y       \tab GS06       \tab <=500m        \tab melissa, parcel, no followup \cr
D8   \tab Y     \tab Y       \tab GS06       \tab 500m..10km    \tab melissa, parcel, flag \cr
D9   \tab Y     \tab Y       \tab GS06       \tab >=10km        \tab disputed_melissa, parcel, flag (gross) \cr
D10  \tab Y     \tab Y       \tab GS03       \tab any           \tab disputed_melissa, zip5, flag (gs03_always) \cr
D11  \tab Y     \tab N       \tab --         \tab --            \tab adece, unknown, flag (melissa_unexpectedly_missing) \cr
D12  \tab N     \tab Y       \tab GS01/GS05  \tab --            \tab melissa, zip4/rooftop, no followup \cr
D13  \tab N     \tab Y       \tab GS06       \tab --            \tab melissa, parcel, flag (melissa_only_interpolated) \cr
D14  \tab N     \tab Y       \tab GS03       \tab --            \tab melissa, zip5, flag (melissa_only_gs03) \cr
D15  \tab N     \tab N       \tab --         \tab --            \tab none, none, flag (both_missing) \cr
}
}

\section{Band boundaries}{

\code{exact} (<10 m), \code{tight} (10-100 m), \code{loose} (100 m-1 km),
\code{drift} (1-10 km), \code{gross} (>=10 km), \code{one_source_only} (only ADECE or
only Melissa present), \code{none} (neither present).
}

\examples{
\dontrun{
raw   <- geocode_read(path = "...", cycle_year = "2026-2027",
                      receipt_date = "2026-03-04")
clean <- geocode_clean(raw)
rec   <- geocode_reconcile(clean)
print(rec)
rec$summary
head(rec$data[, c("row_id", "lat_final", "lng_final",
                   "lat_source", "lat_precision",
                   "coord_agreement_band",
                   "needs_followup_geocoding")])
}

}
\seealso{
\code{\link[=geocode_clean]{geocode_clean()}}, \code{\link[=geocode_config]{geocode_config()}},
\code{\link[=alprek_geocode_resultcode_meaning]{alprek_geocode_resultcode_meaning()}}, \code{\link[=alprek_haversine_m]{alprek_haversine_m()}}.
}
