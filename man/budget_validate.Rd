% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/budget-validate.R
\name{budget_validate}
\alias{budget_validate}
\title{Validate Budget Data Quality}
\usage{
budget_validate(budget_long_obj, tolerance = 1, strict = FALSE)
}
\arguments{
\item{budget_long_obj}{An \code{alprek_budget_long} object from \code{\link[=budget_clean]{budget_clean()}}.}

\item{tolerance}{Numeric. Dollar tolerance for reconciliation mismatches.
Default is \code{1.00}.}

\item{strict}{Logical. If \code{TRUE}, treats warnings as errors (overall result
fails if any warning occurs). Default is \code{FALSE}.}
}
\value{
An \code{alprek_budget_validation} S3 object (list) with elements:
\itemize{
\item \code{passed}: logical overall result.
\item \code{n_errors}, \code{n_warnings}, \code{n_info}: counts by severity.
\item \code{checks}: tibble of individual check results.
\item \code{issues}: tibble of specific rows with problems.
}
}
\description{
Performs comprehensive data quality checks on cleaned budget
data. Returns a validation report with pass/fail status for each check.
}
\examples{
\dontrun{
raw <- budget_read("rptClassBudgets 2023-2024.xlsx", "2023-2024")
cleaned <- budget_clean(raw)
validation <- budget_validate(cleaned)
print(validation)
}

}
