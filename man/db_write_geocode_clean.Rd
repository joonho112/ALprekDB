% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-geocode.R
\name{db_write_geocode_clean}
\alias{db_write_geocode_clean}
\title{Write a cleaned geocode object to DuckDB}
\usage{
db_write_geocode_clean(conn, clean, run_id = NULL, overwrite = FALSE)
}
\arguments{
\item{conn}{A DBI connection from \code{\link[=db_init]{db_init()}}.}

\item{clean}{An \code{alprek_geocode_clean} object.}

\item{run_id}{Optional character scalar overriding the derived
\code{geocode_run_id}. When \code{NULL} (default), derived from \code{clean$meta}.}

\item{overwrite}{Logical. If \code{TRUE}, drop any existing rows for this
\code{geocode_run_id} before writing. Default \code{FALSE} — duplicates are
rejected.}
}
\value{
Invisible character vector of tables written.
}
\description{
Persists \code{alprek_geocode_clean$data} into the
\code{geocode_clean} table, partitioned by \code{geocode_run_id}. The run_id is
either passed explicitly via \verb{run_id =} (preferred) or derived from
\code{clean$meta} using the same \verb{<source>_v1_<YYYY-MM>} scheme that
\code{\link[=geocode_transform]{geocode_transform()}} emits, so a \code{clean -> transform -> write} and a
\code{clean -> write} pair share the same partition.

Also calls \code{\link[=db_write_geocode_lineage]{db_write_geocode_lineage()}} to record one lineage row per
write.
}
