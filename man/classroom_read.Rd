% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classroom-read.R
\name{classroom_read}
\alias{classroom_read}
\title{Read Pre-K Classroom Detail Data}
\usage{
classroom_read(
  path,
  school_year = NULL,
  sheet = "rptRIF",
  remove_footer = TRUE
)
}
\arguments{
\item{path}{Character. Path to the Excel file.}

\item{school_year}{Character. School year in \code{"YYYY-YYYY"} format. If \code{NULL},
attempts to infer from the filename.}

\item{sheet}{Character or numeric. Sheet name or number. Defaults to
\code{"rptRIF"} which is the standard sheet name for classroom detail files.}

\item{remove_footer}{Logical. If \code{TRUE} (default), removes summary/footer
rows (rows where Classroom Code is NA).}
}
\value{
An \code{alprek_classroom_raw} S3 object (list) with elements:
\itemize{
\item \code{data}: tibble of raw classroom data with original column names
(whitespace-squeezed but not renamed).
\item \code{meta}: list of metadata (path, school_year, year, format, etc.).
}
}
\description{
Reads an FCPK Classroom Details Excel file, normalizes column
names, drops footer rows, detects format (legacy vs new), and returns an
\code{alprek_classroom_raw} S3 object.
}
\examples{
\dontrun{
raw <- classroom_read("FCPK Classroom Details 21-22.xlsx")
raw <- classroom_read("24-25 Classroom Details.xlsx", school_year = "2024-2025")
}

}
