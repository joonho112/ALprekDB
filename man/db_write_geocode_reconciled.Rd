% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-geocode.R
\name{db_write_geocode_reconciled}
\alias{db_write_geocode_reconciled}
\title{Write a reconciled geocode object to DuckDB}
\usage{
db_write_geocode_reconciled(conn, reconciled, run_id = NULL, overwrite = FALSE)
}
\arguments{
\item{conn}{A DBI connection from \code{\link[=db_init]{db_init()}}.}

\item{reconciled}{An \code{alprek_geocode_reconciled} object.}

\item{run_id}{Optional character. When \code{NULL}, derived from
\code{reconciled$meta}.}

\item{overwrite}{Logical. Default \code{FALSE}.}
}
\value{
Invisible character vector of tables written.
}
\description{
Persists \code{alprek_geocode_reconciled$data} into the
\code{geocode_reconciled} table, partitioned by \code{geocode_run_id}. Includes
all 10 authoritative columns from \code{\link[=geocode_reconcile]{geocode_reconcile()}} plus the
\code{lineage_id} lineage key from Step 3.1 (so the row-level lineage
survives the round-trip).

Also calls \code{\link[=db_write_geocode_lineage]{db_write_geocode_lineage()}}.
}
