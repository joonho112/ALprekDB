% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-geocode.R
\name{linkage_geocode_applications}
\alias{linkage_geocode_applications}
\title{Link Geocode Panel to Applications Master (Step 6.1)}
\usage{
linkage_geocode_applications(
  geocode_panel,
  applications,
  fuzzy_threshold = NULL
)
}
\arguments{
\item{geocode_panel}{An \code{alprek_geocode_panel} object.}

\item{applications}{An \code{alprek_applications_master} object.}

\item{fuzzy_threshold}{Reserved for a future fuzzy-name fallback (NULL,
ignored currently). When non-NULL and \code{stringdist} is available, the
function will try to match stragglers on Melissa \code{site_name} vs.
\code{organization_name}. Default \code{NULL}.}
}
\value{
An \code{alprek_geocode_linkage_applications} S3 list:
\itemize{
\item \code{data} -- applications data + 12 attached geocode columns
(prefixed \verb{geocode_*}). \code{nrow == nrow(applications$data)}.
\item \code{diagnostics} -- tibble with \code{metric}, \code{value}, \code{group_by}.
\item \code{meta} -- list with \code{linked_at}, run identifiers, match counts.
}
}
\description{
Joins an \code{alprek_geocode_panel} onto an
\code{alprek_applications_master}. For applications that have a resolved
\code{matched_site_code} (renewals + bucket-C new applications), joins on
\verb{(matched_site_code, school_year)}. For bucket-D "new" applications
(no site_code yet), joins on \code{row_id} because the Melissa file's
\verb{2025-2026_new_NNNN} row_ids correspond directly to bucket-D
applications.

\strong{Two-phase join.} The function performs the site_code join first,
then routes the remaining unmatched applications through a \code{row_id}
join against the geocode panel. The two phases are tracked separately
in the diagnostics.

\strong{fuzzy fallback (optional, off by default).} \code{stringdist} (already in
\code{DESCRIPTION}) is available if a future enhancement wants to match on
\code{organization_name} / Melissa \code{site_name} for stragglers. The current
implementation only uses exact key joins; a \code{fuzzy_threshold} argument
reserves the API for later.

\strong{Preserved through the join:}
\code{coord_model_status}, \code{lineage_id} (both applications-side and
geocode-side under \code{geocode_lineage_id}), and \code{geocode_run_id}.

\strong{No row inflation.} \code{nrow(out$data) == nrow(applications$data)}.
Geocode panel is deduplicated to one row per \verb{(site_code, school_year)}
and one row per \code{row_id} (deterministic on \code{geocode_run_id}).
}
\examples{
\dontrun{
lk <- linkage_geocode_applications(panel_g, app_master)
lk$diagnostics
}

}
\seealso{
\code{\link[=linkage_geocode_classroom]{linkage_geocode_classroom()}}.
}
