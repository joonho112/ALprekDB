% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-config.R
\name{geocode_config}
\alias{geocode_config}
\title{Create a Geocoding Processing Configuration}
\usage{
geocode_config(
  path,
  sheet = "Sheet1",
  vendor = "melissa",
  cycle_year,
  delivery_date,
  seed = 20260520L,
  verbose = TRUE,
  authoritative_priority = c("melissa_first", "adece_first"),
  distance_threshold_rules = c("by_resultcode", "flat_100m", "flat_250m"),
  flat_threshold_m = 250L,
  tiered_thresholds = list(GS01 = 50, GS05 = 250, GS06 = 500, GS03 = Inf),
  acceptable_resultcodes = c("GS01", "GS05", "GS06"),
  al_lat_bounds = c(30, 36),
  al_lng_bounds = c(-89, -84)
)
}
\arguments{
\item{path}{Character. Path to the Melissa-returned geocoded xlsx file
(e.g., \code{"ORIGINAL-DATA/2026-03-04_Pre-K Geocoding Melissa/2026-03-04_geocoding_master_Final.xlsx"}).
Required at call site. Existence is \strong{not} checked at constructor
time — \code{geocode_read()} is responsible for the file-system check so
the config object can be assembled in dry-run / test contexts.}

\item{sheet}{Character. Worksheet name within the xlsx file. Default
\code{"Sheet1"} (the v1 Melissa contract).}

\item{vendor}{Character. Geocoding vendor identifier. Default
\code{"melissa"}. Reserved for future multi-vendor support.}

\item{cycle_year}{Character. Cycle year in \code{"YYYY-YYYY"} format
(e.g., \code{"2026-2027"}). Required.}

\item{delivery_date}{Date or character. Date the geocoded file was
delivered by the vendor (e.g., \code{"2026-03-04"} or
\code{as.Date("2026-03-04")}). Required. Character input is coerced to
\code{Date} via \code{as.Date()}; an unparsable string throws.}

\item{seed}{Integer. Random seed for reproducibility (deterministic
tiebreaks, sampling for diagnostics). Default \code{20260520L}.}

\item{verbose}{Logical. Print progress messages? Default \code{TRUE}.}

\item{authoritative_priority}{Character. Which source wins when both
ADECE and Melissa have valid coordinates but they disagree. One of
\code{c("melissa_first", "adece_first")}. Default \code{"melissa_first"}.}

\item{distance_threshold_rules}{Character. How \code{geocode_reconcile()}
decides which (ADECE, Melissa) lat/long pairs require manual
followup. One of \code{c("by_resultcode", "flat_100m", "flat_250m")}.
Default \code{"by_resultcode"} (per-RESULTCODE tiered thresholds — see
\code{tiered_thresholds}).}

\item{flat_threshold_m}{Integer. Flat distance threshold in meters,
used only when \code{distance_threshold_rules} starts with \code{"flat_"}.
Default \code{250L}.}

\item{tiered_thresholds}{Named list. Per-RESULTCODE thresholds in
meters used when \code{distance_threshold_rules == "by_resultcode"}.
Names must be RESULTCODE strings (e.g., \code{"GS01"}); values may be
\code{Inf} (always flag for review). Default
\code{list(GS01 = 50, GS05 = 250, GS06 = 500, GS03 = Inf)}.}

\item{acceptable_resultcodes}{Character vector. Melissa RESULTCODE
values expected to be master-acceptable under the current codebook.
This is retained as configuration metadata and a print surface; the
reconciler enforces \code{acceptable_for_master} from
\code{\link[=alprek_geocode_resultcode_meaning]{alprek_geocode_resultcode_meaning()}} so the CSV remains the source of
truth. Default
\code{c("GS01", "GS05", "GS06")}, matching
\code{melissa_resultcode_codes.csv$acceptable_for_master == TRUE}.}

\item{al_lat_bounds}{Numeric length-2. Alabama latitude bounding box
\code{c(min, max)} for the in-state sanity check. Default \code{c(30, 36)}.}

\item{al_lng_bounds}{Numeric length-2. Alabama longitude bounding box
\code{c(min, max)}. Default \code{c(-89, -84)}.}
}
\value{
An \code{alprek_geocode_config} S3 object — a named list with all
parameters above, ready for consumption by the geocode pipeline.
}
\description{
Creates a typed configuration object that controls the
geocoding module pipeline (read -> clean -> validate -> reconcile ->
followup -> export). The object is consumed by \code{geocode_process()}
and the lower-level step functions (\code{geocode_read()},
\code{geocode_clean()}, \code{geocode_validate()}, \code{geocode_reconcile()}, etc.).
}
\examples{
\dontrun{
cfg <- geocode_config(
  path = file.path("ORIGINAL-DATA",
                   "2026-03-04_Pre-K Geocoding Melissa",
                   "2026-03-04_geocoding_master_Final.xlsx"),
  cycle_year = "2026-2027",
  delivery_date = "2026-03-04"
)
print(cfg)
}

}
