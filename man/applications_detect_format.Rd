% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-detect.R
\name{applications_detect_format}
\alias{applications_detect_format}
\title{Detect ADECE Applications Data Format}
\usage{
applications_detect_format(x, kind = NULL)
}
\arguments{
\item{x}{Either an \code{alprek_applications_raw} object (the recommended input)
or a character vector of raw column names.}

\item{kind}{Character. The data kind being inspected — affects which
marker columns are checked. One of \code{"renewals"}, \code{"new_apps"},
\code{"non_renewals"}, \code{"capacity"}. If \code{x} is an \code{alprek_applications_raw},
this is inferred from \code{x$meta$kind}.}
}
\value{
A character scalar: \code{"cycle1"}, \code{"cycle0"}, or \code{"unknown"}.
}
\description{
Inspects raw column names to determine which cycle's schema
a particular sheet conforms to. Returns one of \code{"cycle1"} (2026-2027
combined-file layout), \code{"cycle0"} (2025-2026 separate-file layout), or
\code{"unknown"}. Used by \code{applications_clean()} to pick the correct column
mapping.
}
\examples{
\dontrun{
raw <- applications_read_renewals(path, cycle_year = "2026-2027")
applications_detect_format(raw)        # likely "cycle1"
applications_detect_format(c("Classroom Code", "Site Code", "Region of Classroom"),
                             kind = "renewals")  # likely "cycle0"
}
}
