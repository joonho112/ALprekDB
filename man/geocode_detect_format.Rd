% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-detect.R
\name{geocode_detect_format}
\alias{geocode_detect_format}
\title{Detect Melissa Geocode File Format}
\usage{
geocode_detect_format(x, sheet = "Sheet1")
}
\arguments{
\item{x}{One of:
\itemize{
\item an \code{alprek_geocode_raw} object (output of \code{geocode_read()}) —
column names are read from \code{x$meta$col_names},
\item a character vector of column names, OR
\item a single character file path to an xlsx file — the first sheet's
header row is read and used as the column names.
}}

\item{sheet}{Character. Sheet name to read when \code{x} is a path. Default
\code{"Sheet1"} (the v1 contract).}
}
\value{
A \code{list} with class \code{"alprek_geocode_format_detection"} and
fields:
\itemize{
\item \code{format} — character scalar, one of \code{"melissa_v1_2026"},
\code{"unknown"}.
\item \code{confidence} — numeric in \verb{[0, 1]}. \code{1} = exact match; \code{0} =
marker columns absent; otherwise Jaccard similarity between
input cols and v1 cols.
\item \code{unknown_columns} — character vector of columns present in the
input that are NOT in the v1 contract (\code{character(0)} when
none).
\item \code{missing_v1_columns} — character vector of v1 contract columns
absent from the input (\code{character(0)} when complete).
\item \code{markers_found} — character vector of marker columns observed
(subset of \code{c("row_id", "LAT", "LNG", "RESULTCODE")}).
\item \code{n_input_cols} — integer count of input columns.
}
}
\description{
Inspects the column names of a Melissa-returned
geocoded delivery and decides whether it matches the v1 contract
(\code{format = "melissa_v1_2026"}) or is unrecognized
(\code{format = "unknown"}). Reports a confidence score in \verb{[0, 1]} and
the set of input columns that are not in the v1 contract.

Used by \code{geocode_read()} (as an upstream gate) and by
\code{geocode_compare_deliveries()} (to label each delivery's format
before diffing). Mirrors the role of \code{applications_detect_format()}
and \code{budget_detect_format()} in their respective modules.

Detection logic:
\itemize{
\item Marker columns (must all be present): \code{row_id}, \code{LAT}, \code{LNG},
\code{RESULTCODE}. Absence of any marker → \code{format = "unknown"},
\code{confidence = 0}.
\item Exact match against the 29 v1 columns (case-sensitive) →
\code{confidence = 1}.
\item Markers present but some v1 columns missing or extra →
\code{confidence} interpolated by Jaccard similarity:
\verb{|input ∩ v1| / |input ∪ v1|}.
\item Threshold: \code{confidence >= 0.5} AND all markers present →
\code{format = "melissa_v1_2026"}. Otherwise \code{"unknown"}.
}
}
\examples{
\dontrun{
raw <- geocode_read(path, cycle_year = "2026-2027")
geocode_detect_format(raw)
# $format = "melissa_v1_2026", $confidence = 1

# From a character vector of column names
v1 <- alprek_geocode_column_map()$raw_col
geocode_detect_format(v1)

# From a file path (reads header row of Sheet1)
geocode_detect_format("ORIGINAL-DATA/2026-03-04_geocoding_master_Final.xlsx")
}

}
\seealso{
\code{\link[=geocode_read]{geocode_read()}}, \code{\link[=geocode_compare_deliveries]{geocode_compare_deliveries()}},
\code{\link[=alprek_geocode_column_map]{alprek_geocode_column_map()}}.
}
