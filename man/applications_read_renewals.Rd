% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-read.R
\name{applications_read_renewals}
\alias{applications_read_renewals}
\title{Read ADECE Renewal Classroom Applications}
\usage{
applications_read_renewals(
  path,
  sheet = "26-27 requests_TW",
  cycle_year,
  receipt_date = Sys.Date()
)
}
\arguments{
\item{path}{Character. Path to the ADECE master xlsx file.}

\item{sheet}{Character. Sheet name. Default \code{"26-27 requests_TW"}
(cycle-1 convention). For cycle-0 separate-file layout, the sheet name
may be \code{NULL} (first sheet).}

\item{cycle_year}{Character. Cycle year label (e.g., \code{"2026-2027"}).
Required.}

\item{receipt_date}{Date or character. Date file received from ADECE.
Default \code{Sys.Date()}.}
}
\value{
An \code{alprek_applications_raw} S3 object (list) with elements:
\itemize{
\item \code{data}: tibble of raw data with column names as-is plus \code{raw_row_index}
and stable \code{lineage_id}
\item \code{meta}: list with kind = "renewals", path, sheet, cycle_year,
receipt_date, file_sha256, n_rows, n_cols, col_names, git_sha, read_at
}
}
\description{
Reads the renewal classroom applications sheet from an ADECE
master xlsx file (cycle-1 layout). Captures provenance (file hash,
git SHA, cycle year, receipt date, sheet, raw row index, row lineage ID)
but does NOT clean or standardize columns — that is \code{\link[=applications_clean]{applications_clean()}}'s
job.
}
\examples{
\dontrun{
raw <- applications_read_renewals(
  path = "Copy of 2026-27 Classroom Applications_tw04202026 (003).xlsx",
  cycle_year = "2026-2027"
)
raw
}

}
