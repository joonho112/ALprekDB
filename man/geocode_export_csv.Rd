% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-export.R
\name{geocode_export_csv}
\alias{geocode_export_csv}
\title{Export Geocode Master / Panel / Reconciled Data to CSV}
\usage{
geocode_export_csv(x, path = NULL, ...)
}
\arguments{
\item{x}{An \code{alprek_geocode_master}, \code{alprek_geocode_panel}, or
\code{alprek_geocode_reconciled} object.}

\item{path}{Character. Output path. If \code{NULL}, auto-generates
\verb{output/geocode/geocode_<run_id>.csv} (master/reconciled) or
\verb{output/geocode/geocode_panel_<run_ids>.csv} (panel).}

\item{...}{Additional arguments forwarded to \code{\link[utils:write.table]{utils::write.csv()}}.}
}
\value{
Invisible character path of the written file.
}
\description{
Writes the \verb{$data} slot of a geocode S3 object to CSV. Mirrors
\code{applications_export_csv()}. Auto-generates \code{path} if \code{NULL}.

\strong{Phase 5 contract.} All Phase 5 exports preserve \code{lineage_id} (Step 3.1
stable row lineage) and \code{coord_model_status} (Step 4.3 ordered factor:
\verb{\{missing, not_model_ready, provisional_followup, model_ready\}}). Rows
with \code{coord_model_status != "model_ready"} remain visible in the export;
downstream SAE consumers MUST distinguish provisional from model-ready
coordinates. This exporter never silently filters rows.
}
\examples{
\dontrun{
mst <- geocode_transform(geocode_reconcile(geocode_clean(geocode_read("..."))))
geocode_export_csv(mst)
geocode_export_csv(mst, "output/custom.csv")
}

}
\seealso{
\code{\link[=geocode_export_parquet]{geocode_export_parquet()}}, \code{\link[=geocode_export_excel]{geocode_export_excel()}},
\code{\link[=geocode_export_rds]{geocode_export_rds()}}, \code{\link[=geocode_export_stata]{geocode_export_stata()}},
\code{\link[=geocode_export_followup_queue]{geocode_export_followup_queue()}}.
}
