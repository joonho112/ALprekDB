% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classroom-validate.R
\name{classroom_validate}
\alias{classroom_validate}
\title{Validate Cleaned Classroom Data}
\usage{
classroom_validate(clean_obj, strict = FALSE)
}
\arguments{
\item{clean_obj}{An \code{alprek_classroom_clean} object from \code{\link[=classroom_clean]{classroom_clean()}}.}

\item{strict}{Logical. If \code{TRUE}, treats warnings as errors. Default \code{FALSE}.}
}
\value{
An \code{alprek_classroom_validation} S3 object (list) with elements:
\itemize{
\item \code{passed}: logical overall result.
\item \code{n_errors}, \code{n_warnings}, \code{n_info}: counts by severity.
\item \code{checks}: tibble of individual check results.
\item \code{issues}: tibble of specific rows with problems.
}
}
\description{
Performs comprehensive data quality checks on cleaned classroom
data. Returns a validation report with pass/fail status for each check.
Validation is advisory — it does not block downstream processing.
}
\examples{
\dontrun{
raw <- classroom_read("FCPK Classroom Details 21-22.xlsx")
clean <- classroom_clean(raw)
val <- classroom_validate(clean)
print(val)
}

}
