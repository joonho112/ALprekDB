% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-core.R
\name{db_init}
\alias{db_init}
\title{Initialize ALprekDB DuckDB Database}
\usage{
db_init(path, read_only = FALSE)
}
\arguments{
\item{path}{Character. File path for the DuckDB database. Use \code{":memory:"}
for an in-memory database (data lost when connection is closed).}

\item{read_only}{Logical. Open in read-only mode? Default \code{FALSE}.}
}
\value{
A DBI connection object to the DuckDB database.
}
\description{
Creates a new or opens an existing DuckDB database for storing
ALprekDB panel data. New databases are initialized with the ALprekDB schema
(metadata and column type registry tables). Existing databases are validated
for schema compatibility.
}
\examples{
\dontrun{
conn <- db_init("output/alprekdb.duckdb")
db_list_tables(conn)
db_close(conn)
}

}
