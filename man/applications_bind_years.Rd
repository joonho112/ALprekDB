% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applications-panel.R
\name{applications_bind_years}
\alias{applications_bind_years}
\title{Combine Multiple Cycles into an Applications Panel}
\usage{
applications_bind_years(..., master_list = NULL)
}
\arguments{
\item{...}{\code{alprek_applications_master} objects to combine.}

\item{master_list}{Optional list of \code{alprek_applications_master}
objects. Alternative to \code{...} for programmatic use.}
}
\value{
An \code{alprek_applications_panel} S3 list with elements:
\itemize{
\item \code{data}: applications-grain long panel (one row per application-cycle)
\item \code{capacity_data}: capacity-grain long panel or NULL if no inputs had capacity
\item \code{cycle_years}: sorted vector of distinct cycle_year values
\item \code{n_cycles}: number of cycles in the panel
\item \code{by_cycle}: per-cycle summary list (\code{cycle_year}, \code{n_apps},
\code{n_capacity}, \code{n_buckets})
\item \code{meta}: \code{binded_at} timestamp + \code{tier_bands} (from first master)
}
}
\description{
Stacks two or more \code{alprek_applications_master} objects
(one per cycle) into a longitudinal \code{alprek_applications_panel}. Mirrors
\code{budget_bind_years()}. Preserves the \strong{two-grain} structure of the
applications module: per-application rows go to \verb{$data}, per-site
capacity rows go to \verb{$capacity_data}.

Each cycle's \code{cycle_year} (from \code{master$meta$cycle_year}) is asserted to
be present and unique across the inputs; the panel rows are sorted by
\code{cycle_year} then by \code{application_id} (or \code{site_code} for capacity).
}
\examples{
\dontrun{
mst_2526 <- applications_transform(rec_2526, cap_2526)
mst_2627 <- applications_transform(rec_2627, cap_2627)
panel <- applications_bind_years(mst_2526, mst_2627)
panel
}

}
