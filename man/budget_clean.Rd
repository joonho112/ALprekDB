% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/budget-clean.R
\name{budget_clean}
\alias{budget_clean}
\title{Clean Budget Data to Long Format}
\usage{
budget_clean(raw, category_groups = NULL, tolerance = 1)
}
\arguments{
\item{raw}{An \code{alprek_budget_raw} object from \code{\link[=budget_read]{budget_read()}}.}

\item{category_groups}{Optional tibble with \code{category_detail} and
\code{category_group} columns. Defaults to the package codebook.}

\item{tolerance}{Numeric. Dollar tolerance for reconciliation diagnostics.
Default is \code{1.00}.}
}
\value{
An \code{alprek_budget_long} S3 object (list) with elements:
\itemize{
\item \code{long}: tibble in long format with budget amounts.
\item \code{totals}: tibble with reconciliation diagnostics per classroom.
\item \code{meta}: list of metadata.
}
}
\description{
Transforms raw budget data into a long-format intermediate
representation: one row per (classroom x category_detail x source_type).
Also produces reconciliation totals for validation.
}
\examples{
\dontrun{
raw <- budget_read("rptClassBudgets 2023-2024.xlsx", "2023-2024")
cleaned <- budget_clean(raw)
cleaned$long      # long format data
cleaned$totals    # reconciliation diagnostics
}

}
