% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-geocode.R
\name{linkage_geocode_classroom}
\alias{linkage_geocode_classroom}
\title{Link Geocode Panel to Classroom Panel (Step 6.1)}
\usage{
linkage_geocode_classroom(geocode_panel, classroom_panel)
}
\arguments{
\item{geocode_panel}{An \code{alprek_geocode_panel} object from
\code{geocode_bind_years()}.}

\item{classroom_panel}{An \code{alprek_classroom_panel} object.}
}
\value{
An \code{alprek_geocode_linkage_classroom} S3 list:
\itemize{
\item \code{data} -- classroom panel rows + 12 attached geocode columns
(prefixed \verb{geocode_*}). \code{nrow == nrow(classroom_panel$data)}.
\item \code{diagnostics} -- tibble with \code{metric}, \code{value}, \code{group_by}. Includes
\code{n_classroom_total}, \code{n_matched}, \code{n_unmatched_geocode},
\code{n_unmatched_classroom}, and coverage broken out by \code{school_year}
and \code{lat_source}.
\item \code{meta} -- list with \code{linked_at}, input panel meta summaries,
\code{n_geocode_rows_in}, \code{n_classroom_rows_in}, \code{match_rate}.
}
}
\description{
Joins an \code{alprek_geocode_panel} onto an \code{alprek_classroom_panel}
so that every classroom-year row inherits the per-site authoritative
coordinates produced by the geocode reconciler. Geocoding happens at
the \strong{site} grain (Melissa addresses are site-level), but the classroom
panel is at classroom-year grain, so multiple classrooms at the same
site share one geocode row.

\strong{Join logic.} Left-join \code{classroom_panel$data} onto a slim view of the
geocode panel keyed by \verb{(site_code, school_year)}. Classroom rows that
have no matching geocode row keep \code{NA} for the 12 attached columns; the
per-classroom diagnostic surfaces the unmatched count.

\strong{Preserved through the join:}
\itemize{
\item \code{coord_model_status} -- the ordered factor from \code{geocode_reconcile()}.
Analysts MUST see model-readiness at the classroom-year row, otherwise
a downstream SAE pipeline cannot honor the "do not promote
provisional rows" rule.
\item \code{lineage_id} -- both the classroom-side (kept verbatim if present)
and the geocode-side (attached as \code{geocode_lineage_id}). The two are
separate strings; downstream models choose which one to anchor on.
\item \code{geocode_run_id} -- panel-stable identifier from
\code{geocode_transform()}.
}

\strong{No row inflation.} \code{nrow(out$data) == nrow(classroom_panel$data)}.
If the geocode panel happens to have multiple rows per
\verb{(site_code, school_year)} (e.g., a renewal site re-geocoded in two
release cycles), the join deduplicates by taking the most recent run
per (site_code, school_year) using a stable \code{geocode_run_id} sort
(lexicographic on \code{YYYY-MM} token) so the join stays 1:1.

\strong{Renaming.} All 12 attached columns are prefixed \code{geocode_} (e.g.,
\code{geocode_lat_final}, \code{geocode_lat_source}, \code{geocode_lineage_id},
\code{geocode_run_id}) to avoid collisions with classroom-panel columns
like \code{latitude} or \code{lineage_id}. The classroom panel's own ADECE
\code{latitude} / \code{longitude} columns are left untouched (Decision §11.4:
escape hatch / inspection).
}
\section{Behavior on missing keys}{

\itemize{
\item Classroom rows with \code{site_code = NA} cannot join; they appear as
unmatched. The diagnostic \code{n_unmatched_classroom} includes them.
\item Geocode rows with \code{site_code = NA} (the bucket-D \verb{_new} cohort) are
excluded from the classroom join entirely; they are surfaced
through \code{linkage_geocode_applications()} instead.
\item Geocode rows whose \verb{(site_code, school_year)} does not appear in
the classroom panel show up in \code{n_unmatched_geocode}.
}
}

\examples{
\dontrun{
panel_g <- geocode_bind_years(geocode_master)
panel_c <- classroom_bind_years(c2122, c2223, c2324, c2425)
lk <- linkage_geocode_classroom(panel_g, panel_c)
lk
lk$diagnostics
}

}
\seealso{
\code{\link[=linkage_geocode_applications]{linkage_geocode_applications()}}, \code{\link[=geocode_bind_years]{geocode_bind_years()}},
\code{\link[=classroom_bind_years]{classroom_bind_years()}}.
}
