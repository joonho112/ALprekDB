% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkage-applications.R
\name{linkage_applications_classroom}
\alias{linkage_applications_classroom}
\title{Link Applications Master to Classroom Panel}
\usage{
linkage_applications_classroom(
  applications,
  classroom,
  target_school_year = NULL,
  attach_capacity = NULL
)
}
\arguments{
\item{applications}{An \code{alprek_applications_master} object (from
\code{applications_transform()}).}

\item{classroom}{An \code{alprek_classroom_panel} object. Joins only happen on
the school year(s) inferred from the application's \code{cycle_year} (e.g.,
\code{cycle_year = "2026-2027"} maps to a target school year of
\code{"2025-2026"} for renewal-prior linkage, but here we use the cycle's
own year for \code{applied_this_cycle} semantics).}

\item{target_school_year}{Optional character. The school_year value in
\code{classroom$data} to attach applications context to. Default: derive
from \code{applications$meta$cycle_year} (e.g., \code{"2026-2027"} ->
\code{"2026-2027"}). The "prior" classroom panel used by reconcile is one
year behind; this join uses the \emph{current} cycle's classroom row.}

\item{attach_capacity}{Logical. Attach \code{capacity_utilization} /
\code{waitlist_ratio} / \code{is_oversubscribed} via \code{site_code}? Default \code{TRUE}
when applications has \code{capacity_data}.}
}
\value{
An \code{alprek_applications_linkage} S3 list:
\itemize{
\item \code{classroom_level}: tibble - \code{classroom$data} rows for the
\code{target_school_year} joined with application columns (left-join, so
classrooms that didn't apply still appear with \code{applied_this_cycle = FALSE})
\item \code{unmatched_applications}: tibble - bucket D rows (truly new
applications with no \code{matched_classroom_code} and no
\code{matched_site_code}); downstream geocoding package will resolve
these
\item \code{diagnostics}: tibble - join counts (n_classroom_rows,
n_applications_in, n_matched, n_only_classroom,
n_applications_direct_classroom, n_applications_site_aggregated,
n_only_application_unmatched)
\item \code{meta}: \code{linked_at}, \code{cycle_year}, \code{target_school_year},
\code{attached_capacity}
}
}
\description{
Joins an \code{alprek_applications_master} object to an existing
\code{alprek_classroom_panel}. For each classroom-school-year row in the
panel, attaches the per-application columns describing whether the
classroom \strong{applied this cycle}, in which \strong{bucket}, and (where
available) the \strong{tier carry-forward} and \strong{capacity-grain indicators}
for the matching site.

Join logic:
\itemize{
\item \strong{Renewals (bucket A/B)}: exact join on \verb{(matched_classroom_code, cycle_year to school_year_target)}. Carries \code{tier_prev_dollars},
\code{tier_prev_rank}, \code{tier_prev_band}, \code{bucket}, \code{match_method},
\code{match_score}.
\item \strong{New applications (bucket C/D)}: bucket C is aggregated to the
matched site via \code{matched_site_code}, even when reconciliation also
recorded the nearest \code{matched_classroom_code}. Bucket D has no matched
site and remains in \verb{$unmatched_applications}.
\item \strong{Capacity-grain merge}: optional left-join on \code{site_code} to attach
\code{capacity_utilization}, \code{waitlist_ratio}, \code{is_oversubscribed}.
}

This is the \strong{data-layer} linkage only - it carries application context
into the panel without computing geocoded/ACS/Bayesian features (those
are downstream packages).
}
\examples{
\dontrun{
mst <- applications_transform(rec, cap_clean)
panel <- readRDS("output/classroom/classroom_panel_2021-2025.rds")
lk <- linkage_applications_classroom(mst, panel)
lk
lk$classroom_level
}

}
