% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode-read.R
\name{geocode_read}
\alias{geocode_read}
\title{Read Melissa-Returned Geocoded Master File}
\usage{
geocode_read(
  path,
  sheet = "Sheet1",
  cycle_year,
  receipt_date = Sys.Date(),
  source = "melissa",
  verbose = TRUE
)
}
\arguments{
\item{path}{Character. Path to the Melissa-returned geocoded xlsx
file (e.g.,
\code{"ORIGINAL-DATA/2026-03-04_Pre-K Geocoding Melissa/2026-03-04_geocoding_master_Final.xlsx"}).
Required. Existence is checked at call time with an informative
error.}

\item{sheet}{Character. Worksheet name within the xlsx file. Default
\code{"Sheet1"} (the v1 Melissa contract).}

\item{cycle_year}{Character. Cycle year label in \code{"YYYY-YYYY"}
format (e.g., \code{"2026-2027"}). Required.}

\item{receipt_date}{Date or character. Date the geocoded file was
received from Melissa (e.g., \code{"2026-03-04"} or
\code{as.Date("2026-03-04")}). Default \code{Sys.Date()}.}

\item{source}{Character. Geocoding source label, used in provenance
tracking. Default \code{"melissa"} (the only supported vendor in
v0.8.0). Reserved for future multi-vendor support.}

\item{verbose}{Logical. Print progress messages? Default \code{TRUE}.}
}
\value{
An \code{alprek_geocode_raw} S3 object (list) with elements:
\itemize{
\item \code{data}: tibble of raw Melissa data (29 columns as-is from the
xlsx, plus \code{raw_row_index} and \code{lineage_id} columns for stable
row tracking). LAT/LNG remain character per Melissa source contract.
\item \code{meta}: list with \code{path}, \code{sheet}, \code{cycle_year}, \code{receipt_date},
\code{source}, \code{file_sha256}, \code{file_basename}, \code{git_sha}, \code{n_rows},
\code{n_cols}, \code{col_names}, \code{read_at}, \code{lineage_id} (compatibility
mirror of \verb{$data$lineage_id}), \code{raw_row_index} (\code{1:nrow}).
}
}
\description{
Reads the Melissa-returned geocoded xlsx file (v1 contract,
29 columns) and captures provenance (file SHA-256, git SHA, cycle
year, receipt date, sheet, raw row index, row lineage ID). Does
\strong{not} clean, normalize, or coerce — that is \code{\link[=geocode_clean]{geocode_clean()}}'s
job. In particular, \code{LAT} / \code{LNG} are preserved as character per the
Melissa source contract (they are coerced to numeric only by
\code{\link[=geocode_clean]{geocode_clean()}}).

Mirrors the read+provenance pattern established by
\code{\link[=applications_read_renewals]{applications_read_renewals()}} et al., except that the geocode
module is the \strong{first consumer of row-level geocode lineage} introduced
for v0.8.0. \code{lineage_id} is stored directly in \verb{$data} as a stable
row key and mirrored in \verb{$meta} for compatibility.
}
\examples{
\dontrun{
raw <- geocode_read(
  path = file.path("ORIGINAL-DATA",
                   "2026-03-04_Pre-K Geocoding Melissa",
                   "2026-03-04_geocoding_master_Final.xlsx"),
  cycle_year = "2026-2027",
  receipt_date = "2026-03-04"
)
raw
}

}
